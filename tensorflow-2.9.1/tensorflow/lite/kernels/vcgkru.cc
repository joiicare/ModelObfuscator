/* Copyright 2017 The TensorFlow Authors. All Rights Reserved.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
==============================================================================*/
#include "tensorflow/lite/kernels/internal/optimized/integer_ops/conv.h"

#include <stddef.h>
#include <iostream>
#include <cstdint>
#include <vector>

// Only use multi-threaded Eigen if ruy is disabled.
#if !defined(TFLITE_WITH_RUY)
#define TFLITE_WITH_MULTITHREADED_EIGEN
#endif

#include "tensorflow/lite/c/builtin_op_data.h"
#include "tensorflow/lite/c/common.h"
#include "tensorflow/lite/kernels/cpu_backend_context.h"
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
#include "tensorflow/lite/kernels/eigen_support.h"
#endif
#include "tensorflow/lite/kernels/internal/compatibility.h"
#include "tensorflow/lite/kernels/internal/types.h"
// b/131835803 forces us to include multithreaded_conv.h before optimized_ops.h
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
#include "tensorflow/lite/kernels/internal/optimized/multithreaded_conv.h"
#endif
#include "tensorflow/lite/kernels/internal/optimized/optimized_ops.h"
#include "tensorflow/lite/kernels/internal/quantization_util.h"
#include "tensorflow/lite/kernels/internal/reference/conv.h"
#include "tensorflow/lite/kernels/internal/reference/integer_ops/conv.h"
#include "tensorflow/lite/kernels/internal/tensor.h"
#include "tensorflow/lite/kernels/internal/tensor_ctypes.h"
#include "tensorflow/lite/kernels/internal/tensor_utils.h"
#include "tensorflow/lite/kernels/kernel_util.h"
#include "tensorflow/lite/kernels/padding.h"
#include "tensorflow/lite/util.h"

namespace tflite {
namespace ops {
namespace custom {
namespace vcgkru {

// This file has 4 implementation of Conv.
enum KernelType {
  kReference,
  kGenericOptimized,  // Neon-free
  // kMultithreadOptimized is a mixture of an Eigen-based kernel when threads
  // are available and kGenericOptimized when we must use only one thread.
  kMultithreadOptimized,
  // The kernel uses use CBLAS interface for matrix multiplication.
  // It's fast when an optimized CBLAS implementation is available (e.g. Apple
  // Accelerate Framework), and it's slow when falling back to naive
  // implementation.
  kCblasOptimized,
};

const int kTensorNotAllocated = -1;

static constexpr size_t kMaxIm2colBufferSizeMobile = 1024 * 1024 * 1024;  // 1GB

int8_t filter_r   aw[9600]={-7, 11, -1, -8, 1, -8, 2, -7, 3, -28, 1, -16, 16, 17, -31, 7, -15, -16, 13, -5, -2, -12, 3, 8, 10, 7, 5, 1, 14, 11, -11, -9, -2, 9, -8, 15, 65, 23, 23, 16, -18, 10, 5, -21, 25, 13, 5, 6, 59, 12, 5, -8, 26, -17, 1, -5, -65, 7, 7, -8, -13, -14, -4, 10, 17, -13, -3, -9, -6, 17, -17, 24, 2, 22, -9, 81, -26, 15, 4, 12, 3, -9, -6, -14, 14, -14, -13, -7, -14, -6, -1, -7, 9, -17, -10, 7, 9, -1, -12, -16, -3, 10, 6, -76, -5, -37, -21, 7, -3, -2, 0, 11, -8, -12, -16, -12, 16, 20, 23, 0, -30, -6, -14, -10, 24, 12, 2, -3, 14, -12, -4, 8, 8, -5, -13, 15, -34, -2, -23, 8, -33, -20, 8, -10, -5, 1, 9, -6, 1, 9, -21, 0, -2, 31, 5, -4, 2, -29, 127, 6, -8, -18, 0, 27, -10, -10, 11, 54, -33, 37, 17, -7, -13, -29, -38, -31, 36, 9, -3, 1, 4, 3, -1, -1, 37, 24, -4, -15, -6, 10, 3, 17, 3, 26, -18, 5, 0, 21, -1, -28, -10, 3, 18, 0, 1, -1, 5, 0, 8, 6, -2, -7, 10, -1, -15, -2, -1, 23, -104, 58, -8, 25, 6, 16, -4, 7, 5, 3, -11, -105, -27, 8, 1, 4, -29, -5, -5, -10, -3, 30, -12, -4, -5, 15, -53, 15, 14, -24, 1, -26, 17, -9, -79, 32, -15, -2, 0, -14, -33, -11, 21, -99, -46, -20, 8, -17, 10, 17, -21, 80, 24, -30, -22, -13, 4, -20, 17, -61, -11, -11, -13, 42, -17, 22, 44, 13, -1, 44, -13, -30, 2, 20, 7, 33, 26, 51, -17, -54, 6, 12, -23, -21, 0, -61, 43, -28, -58, 2, 1, 28, -1, -24, 52, -14, -30, 13, 25, -68, 14, 3, -15, 58, 0, 55, 18, -8, 34, 26, -26, -51, -13, 22, -17, 0, -77, 21, -10, -1, -14, 41, -15, -17, -4, 20, 83, 1, 2, 23, -18, 12, 18, -63, -22, -48, 20, -6, 4, 13, 60, -16, -24, 47, 27, -20, -33, 24, 14, -5, 38, 41, -12, 36, -11, 29, 20, 53, -44, -71, 2, 32, -9, 7, -16, -18, -83, 10, 17, -47, 5, -52, -6, -1, -55, 10, -1, 20, -12, -82, 29, -11, 1, -32, 4, -10, 57, -45, -18, -19, -21, -48, -66, -17, -72, -43, 3, -48, 1, 3, -12, 17, -15, 23, 56, -56, 1, 1, 55, 5, -56, -38, 30, -54, -31, -36, 33, -12, 22, 20, -37, 15, -31, -89, 53, -53, 41, -1, -27, -31, -16, -4, -10, -24, 31, 33, -86, 30, -3, 4, 4, 61, -40, 0, -29, -50, 1, 127, -43, 37, -71, 28, -48, -22, 28, 42, 14, -62, -15, -16, -14, 37, 19, 36, -10, -34, -28, -3, 41, 1, 46, 5, 5, -33, 47, -37, 51, -11, -2, 37, -40, -15, -23, 10, -14, -76, -52, -45, -11, 73, 46, 16, -35, -26, 33, -10, -36, -5, 61, -26, -47, -22, 59, -121, 28, 39, 38, -71, -41, -8, 72, -38, 55, 21, -74, -6, -7, -17, -13, -15, 45, 32, -42, 24, 49, -52, -17, -22, -23, -75, 16, -5, 68, -14, -65, -82, 11, 37, 49, -19, -57, -12, -30, 51, 54, 33, 11, 23, 61, 13, 43, -41, 18, 9, -37, 13, 28, -76, -27, 107, 30, 16, 6, -16, -23, -71, 4, 59, 109, 32, 46, 79, -15, 43, -26, -5, -32, 7, -20, -17, -19, -98, 61, -1, 18, 16, 38, -53, 29, -46, -19, 70, -71, 50, 127, 26, -4, 23, -7, -18, -10, 2, 1, 11, -52, 47, 28, 24, -86, 24, 16, 34, 17, -50, -19, 54, 64, -6, -2, 57, 28, -11, -70, -40, -13, 13, 19, 60, -8, 11, -3, -34, -22, -32, 23, 82, 92, 18, -31, -70, 20, 32, -3, -14, -3, -29, -47, -13, -48, -29, 30, 14, 20, -49, 58, -90, -19, 65, -28, -67, -6, -44, 89, -35, 2, -34, -97, 69, -22, 39, -5, 26, 25, -17, -16, -59, 5, -5, 3, 60, -78, -25, 66, -111, -91, 7, -72, 16, -9, 33, -46, -26, 7, -37, 20, 50, 35, 10, 31, 24, 41, 9, 29, 51, -100, 7, -15, -87, -15, 62, 77, -18, 25, -9, -96, 31, 38, 24, 34, 35, -65, 55, -7, 39, -20, -110, -9, -13, 127, 37, 72, -27, 4, -89, 67, 8, -13, -28, -3, 10, -48, -58, -21, 66, -26, -29, -60, -52, 28, 15, -32, 75, -8, -28, -49, -59, -23, 12, -9, 29, -43, -10, 27, 28, 80, 42, 33, 56, -10, -77, 33, -12, -17, -60, -44, 14, 58, 29, -93, -5, -8, 9, 25, 25, 18, 26, 48, 30, -25, 13, -7, -14, -8, -68, 23, 24, 50, 36, -84, -13, 13, -101, 34, -20, -39, -16, -20, 6, 76, 0, 7, -34, 40, -12, 7, -10, -31, -32, -37, -42, -25, 90, -36, 12, 51, 22, -38, 61, 31, -8, -99, -40, 27, -15, 45, 31, -49, 76, -25, -4, 53, 41, -5, 26, -103, 1, 33, 16, -78, 19, -77, -127, 29, -60, -17, -6, 16, 14, -18, 5, 2, -7, 36, -7, -33, -10, 25, 49, 6, 21, -25, 46, 15, 54, -19, 82, 62, 34, -6, 63, 1, 44, -8, 16, -26, -14, -32, 73, 17, -91, -36, 7, 1, -72, -1, -100, -5, -20, 49, 35, -44, -25, 46, 38, -38, -12, 40, 16, -42, -14, -33, -12, -5, -11, -6, -48, -11, 13, -78, -26, -10, -110, -38, -44, -38, -17, -45, -3, -7, -67, 16, -6, -21, -27, -69, 1, -96, 2, 17, 13, 6, -26, 9, -50, 18, 18, 4, 40, 28, -49, -16, 44, -23, -12, -5, 21, -67, -21, -12, 1, 53, 6, 32, -37, 2, 27, 47, -10, 20, -29, -9, 14, 4, 46, -33, 12, -68, -4, 35, -2, 2, 0, 10, 85, -26, -21, -5, 57, 58, 12, -17, 28, 29, 17, -5, 1, 18, -10, 22, -10, -8, -10, 18, 16, -20, 1, -2, -28, 8, 31, -7, -18, 21, 16, 11, 35, -40, 16, 25, -4, 36, 11, 11, -91, -10, 42, 0, -9, 16, -32, -25, 5, -3, -34, -29, -3, 10, 127, -21, -24, 13, -25, -4, -8, -10, 15, 29, -3, -29, 6, 4, -59, 12, -20, 16, 2, 21, -6, -27, 7, 8, 36, -33, -17, -70, 11, -3, -64, 1, 17, -2, 27, 48, 2, -35, 8, 8, 45, -10, 1, 32, 6, 24, 1, -61, -3, 7, 12, 18, 11, -18, -47, 5, 34, 10, 1, 56, -21, 10, -70, 9, 11, -12, 8, -4, 38, -48, -12, -25, 15, 53, 14, -4, 6, 5, 5, -49, -56, 1, 2, -37, 12, -27, 28, 11, 0, -7, 10, -25, 37, 8, -12, -9, 62, 41, 25, 35, -61, -17, 39, -1, 45, -23, 1, 8, 6, -29, -33, -2, -59, -13, -8, -62, -26, 11, -3, -44, 13, -19, 26, -11, 0, 6, 17, -39, 7, 48, -9, -17, 26, 22, -2, 7, 6, 14, 44, -18, 7, -42, 0, -3, -34, 8, 7, -14, 39, -59, -8, 18, -32, 38, -7, 36, 16, -69, 12, 108, 73, -94, -76, -7, 32, 11, 84, -66, -9, 15, -46, 45, 14, 8, -79, 19, -3, -32, 11, 55, -13, 11, -29, -17, 27, -26, 1, 2, 28, -37, 4, -16, 24, 16, -8, -10, 18, -52, 12, -6, 12, 3, 110, 29, -26, -35, 40, 60, -59, -38, 3, -68, 22, -71, 10, 36, -22, -8, 62, -44, 48, 23, -73, 6, 3, 5, 18, 9, 126, 17, -97, 12, 1, 41, -71, 4, 1, 55, -94, 116, 6, 9, 15, -68, 48, -81, 8, 11, 7, -88, -22, -62, -18, -4, 1, 4, -59, 8, -51, -2, 30, 3, -13, 59, -20, 37, 25, 14, 11, -12, -8, 2, 17, 5, -4, -35, -17, -1, -21, 29, -74, -5, -49, 69, -12, -7, -97, 35, 69, 43, -59, -19, 28, 3, 37, -92, 18, -66, -24, -5, -13, -20, 68, 6, -26, -5, 16, -1, -2, 67, 45, -62, -33, -42, 0, -52, 18, 41, 56, 4, 127, -2, -87, 36, 11, 11, 24, -15, -67, 4, 86, 26, 9, -13, -16, 11, 1, 19, -6, 38, 12, 14, -35, -19, -15, 6, -3, -1, -66, 19, 8, -32, -28, -37, -61, -27, -20, -45, 7, 84, 97, 6, -14, -24, 31, 12, 35, 5, -3, -87, 108, -38, -4, 22, 2, -58, -22, -23, -25, -7, -11, -37, -50, -30, 45, -11, 14, -12, -9, 1, 16, 95, -17, 7, -17, 6, -36, -28, 18, -10, 26, -14, -4, 23, -30, 10, -18, -4, 3, 1, 15, 24, 22, -5, -27, 0, -9, 5, 1, -102, 12, -43, 9, -15, -29, -10, -3, -8, -9, -22, -1, 14, 30, -26, -17, 16, 12, -1, -10, -5, -11, 13, -36, -17, 7, 2, -1, 22, 12, -18, -18, -7, -37, -9, -29, -4, 17, 6, -5, -25, -8, -20, -2, 7, 12, -8, -74, 17, 16, 13, -12, -11, -20, -23, 3, 5, -18, 6, -17, -16, 21, -24, 33, -21, -15, -9, -16, -20, 50, -12, 90, -33, 4, 4, 22, -16, 9, -17, -2, -4, -6, -45, -5, 25, 11, -28, 14, 0, -12, -35, -7, 12, 25, -15, 24, 22, -2, 37, -12, -2, 3, 13, -8, -4, 17, 28, -13, -3, -4, 4, 24, 5, -16, -5, 20, -21, 16, -2, -38, 54, -1, 18, -21, -40, 22, 18, -59, -12, 77, -8, -69, -25, 127, -14, -1, 6, 1, -2, -1, -36, 21, 51, -12, -15, 17, -20, -17, 18, -1, 43, 37, -70, -26, 5, 12, -3, -28, 29, 77, -1, 18, -3, 13, 7, 29, 19, 10, -8, 0, -29, 23, 5, -4, -18, 15, -7, 27, 48, -21, 29, 10, 1, 2, 62, 56, 22, 14, -19, 10, -1, 12, 10, 29, -1, 59, -17, -31, 16, -25, -15, -3, -8, -18, -8, 23, -23, -8, -21, -11, 32, 21, -20, -5, 20, -31, -42, 2, -30, -6, 33, 6, -3, -25, 43, 26, -20, 13, 27, -59, 5, -33, 39, 127, -42, -37, 18, -3, -5, 40, -20, -37, 18, 36, 18, 3, 48, -69, -3, -44, 42, -59, 17, -3, -22, 26, 26, -12, -24, 25, -6, -7, 4, 6, -27, 5, 25, 9, 8, -22, -11, 2, 28, -50, -2, 26, -56, 44, -2, 40, 9, -44, -82, 18, -29, 50, 46, 33, 17, 14, 25, -14, -20, -7, -18, 26, -9, -3, 38, -6, -3, 10, -87, 21, -25, -39, -4, 24, 16, -20, -33, -5, 0, 8, 27, 1, 23, -37, -23, 26, -31, 23, 20, -7, 7, 61, -15, -41, -21, -1, 7, -42, -33, 33, -2, -24, 47, 0, 107, -6, 2, 48, 19, 9, 5, -23, -21, -1, -99, 0, -3, 8, 1, 49, -15, -34, -4, -25, 29, 9, -8, -71, 33, 5, -15, 35, 40, 38, 13, -7, 18, 4, -51, -9, -36, -13, 3, 30, -18, -54, -27, 54, 0, -13, -25, -81, -6, 3, 65, 32, 6, -20, 15, -28, 4, 5, 9, -21, 118, -3, -3, -48, -64, 7, -11, -32, 83, 1, -14, 37, -73, -35, -30, 6, -8, -20, 0, -8, -14, 8, 1, -4, -41, -8, -27, 8, 75, -3, -15, 22, 11, -47, -47, 24, -52, 56, 38, 8, 46, -42, -9, 1, -47, -4, -1, 52, 1, 6, -5, -30, 1, -10, 2, 1, 22, 11, -1, 11, -8, -30, -12, 7, -10, 0, 1, 16, -14, 21, 6, 20, -12, -36, -1, -6, 21, 37, -5, -8, 20, 33, 15, 0, 88, 3, -10, 22, -20, -4, -28, 9, -2, -10, 6, 1, -15, 11, 77, -19, 38, -1, 12, 2, -13, -14, 0, -9, -28, -39, 19, -9, 28, -1, 10, 10, -10, 10, 3, -28, -2, -46, -4, -61, 5, 1, -19, 2, 17, 37, 34, -9, -5, -16, -20, 16, -16, -4, 20, -23, -9, -20, 6, -6, -3, -8, 9, -41, -11, 12, -13, -98, -7, -40, 44, -1, 13, -2, 5, -1, -8, 7, 19, -19, -63, 24, 4, -40, 15, -8, -17, -2, 45, -10, 40, 24, -21, -6, -31, 4, 11, -5, -36, 26, 13, -2, 17, 44, 37, -12, -8, 12, -7, 3, -27, 26, -18, 0, -16, -1, 7, 127, -7, -6, 9, -20, -97, 6, -72, 32, -29, 3, -6, 35, -6, -17, 9, -18, -5, 4, -3, 27, -10, -13, -14, -45, 15, -3, 16, 24, -21, -12, 24, 13, -2, 4, -9, 27, 30, 37, -5, -33, 17, -10, 18, 3, -38, 34, 16, 2, 3, -1, -4, 0, 1, -8, -12, -9, -10, -4, -61, 71, -48, -16, -34, -44, -72, 46, -8, -32, -29, 5, -10, -17, -2, 5, -9, 59, -10, -33, 9, 1, -1, 4, -11, 10, 11, 11, -110, -38, 12, -19, 16, 58, 44, 1, -31, 6, 84, -3, 20, -12, 0, -65, 16, 18, 70, 17, -116, 45, 39, 11, -59, 18, -6, -14, -27, 16, 73, 74, -31, -60, 6, 15, 32, -5, -49, -10, -2, 59, -17, -16, -18, 103, 9, 33, 1, -41, -39, -36, -44, 35, 37, 29, 3, -75, 80, -9, 29, 45, 77, -25, -3, -98, 27, 122, 22, 17, -2, -127, -27, 51, -36, -16, 48, 17, -115, -44, -44, -25, -55, 121, -36, 3, -39, -18, 21, 15, -44, 86, -3, 6, 72, 42, 72, -56, 0, -14, 19, 81, 0, 13, -3, 81, 63, 42, 59, 32, -28, -2, -114, 15, -22, -61, -14, -25, -17, -73, 4, 0, -6, -102, -12, -46, 23, -42, 8, 93, 11, -5, -36, -14, -5, 8, -8, 5, 52, 7, 14, 7, -55, -60, 9, -99, 18, 15, 4, -64, 11, 17, 18, -8, -14, -18, 93, -37, -23, 103, 8, -12, 41, 1, -16, 3, -16, -1, 7, 31, 8, -21, -59, 14, -4, 60, 15, -32, 20, -49, 96, 27, 35, 26, 5, -3, 15, 119, -11, -33, 7, -48, -3, 27, 28, -73, 17, -31, -19, 5, -58, 46, -31, 31, 12, -22, -52, -25, 33, 9, -13, -8, -7, 11, 3, -52, 38, 3, -8, -5, -5, -18, -62, -6, -41, -19, -10, -20, 34, -1, 36, 11, -23, -1, 21, -22, -31, -3, -10, 24, -17, 34, 14, -21, 23, -22, -55, 38, -46, 42, -23, -16, 43, -5, 25, 16, 5, 12, 54, 32, -25, 29, 27, 30, 40, -7, -1, 0, 49, -45, -41, -16, 6, 30, 62, 20, -41, 13, -4, 71, -3, 33, 20, -8, 20, 14, -127, -28, 1, 45, 101, -34, -22, 43, -9, -2, 22, 64, -67, -43, -15, 49, -38, 8, -21, 49, 21, 13, 8, 34, -17, 32, 42, 31, 31, 7, -27, 38, -13, 62, 55, -22, 22, -16, -21, -7, -30, 46, -22, 24, 30, -17, -7, 4, -17, 0, -27, 19, 12, -38, 9, 34, -2, -9, -6, -33, -50, 12, 20, -63, 38, 42, -14, 12, -35, 18, -65, -13, -9, 35, 33, -17, -6, -23, -13, 56, -10, 82, 17, -34, -27, -6, 2, -24, -8, 40, 8, -8, -9, 3, 95, 7, -23, 5, 12, -3, 21, 19, 0, -43, -10, -43, 20, -33, -60, -22, -1, -29, -2, 3, -103, 55, 7, -32, 3, 11, -13, -55, -4, -48, -6, 73, 19, -35, 2, -18, 29, 28, -18, -105, 36, -26, -10, 29, 4, -5, 74, 3, -2, 13, 64, -70, -53, 35, 7, 52, -20, 26, 24, -68, -14, -5, 33, 21, 17, -22, 25, 29, 22, 10, 26, -39, -111, -34, 46, -19, -31, 22, 24, 8, -22, -7, 68, 1, 35, -60, -11, -9, -42, -27, -2, -9, 13, -3, -10, -19, -23, 13, 6, -4, -17, 38, 11, -74, 4, -8, 30, 34, 8, -29, 6, -41, -21, 38, -30, -38, -29, -25, 10, 52, -11, 11, 4, 48, -29, -59, -29, -6, -7, 14, -29, -37, 4, 40, -101, -32, -8, -22, 32, -18, 23, -14, 24, 16, -32, -70, 21, -6, 0, 23, 127, 37, 38, 47, 27, 13, 11, -34, -90, -41, 22, -17, 45, 9, 24, 66, -4, 31, -36, -11, 28, -17, 85, 54, 25, 28, -30, -16, 9, 34, 48, -37, 27, -34, -4, -24, 11, 53, -36, 3, 27, 27, -50, -22, 19, 0, -53, 8, -35, -19, -9, 4, 0, -11, 28, 39, 44, -36, -8, -33, 22, -30, -7, 13, -33, 89, -61, -50, 5, 62, -26, -9, 62, -7, -32, 35, -38, 76, -2, 0, 58, 5, 1, 9, 17, 34, -17, -12, -17, -15, -13, -28, -60, -50, 7, 6, 19, 25, -23, -86, -12, 46, 4, 12, 22, -19, -33, -42, 36, -32, -77, 89, 69, -8, 16, -10, -45, 19, 14, 5, -21, 50, 43, 2, -27, 2, -8, -4, -39, -122, -10, 10, 6, 8, -49, -23, -11, -5, 2, 18, 110, 48, 10, -25, 10, -14, 8, 47, -23, 0, 6, 11, -7, 11, 20, -63, -15, 17, -8, -29, 26, 6, -73, -56, -14, 24, 6, 31, 28, 63, 20, -50, 33, -7, 8, -21, -14, -27, -71, -14, 30, 2, 25, -5, -7, -9, -29, -4, -22, 4, 20, 26, -44, 9, 20, 48, 23, -69, -33, -57, 72, -63, 17, 64, 23, 43, 7, 1, -10, -61, 51, 39, 38, -9, -25, -52, -17, 44, 98, 2, -47, 1, 8, -87, -23, -31, -39, -27, 28, 8, -13, 127, 62, 28, -38, 0, -26, 66, 41, -70, 60, -57, 45, -51, 11, 30, -16, 8, 43, 2, 13, -4, 36, -6, 9, -54, 30, -25, 4, -44, 80, 2, -32, -10, -39, -37, -20, 8, 46, -43, -74, -36, 35, 68, -9, 8, 25, -41, 10, 32, -15, 13, 32, 4, 2, 119, -43, 10, -6, -15, 12, -8, -7, 14, 7, -17, 0, 15, -24, 70, -69, 46, -39, 11, 52, -10, -20, 29, 52, 31, -9, 26, -40, -28, -11, -25, 49, -78, -3, -7, -76, -3, 74, 29, -16, -44, -25, 3, -51, 20, -6, 2, -65, 31, -53, 3, -15, -25, -10, -38, 24, -49, 17, -1, 24, 41, 69, 0, -5, 21, -43, -29, -62, -11, -13, 9, 10, 29, 7, -7, 1, 19, 7, -29, 51, 48, 1, -19, 89, 49, 17, -17, 91, 39, -66, 68, 40, 17, -30, -3, 6, 83, -34, -71, -38, 36, 0, 52, -18, -17, 70, 8, -11, 24, -23, 30, -32, -45, -2, 5, -43, 6, 27, 77, -13, 14, -50, -25, 48, 0, -32, -28, -99, -7, -20, 37, 34, 65, 15, -16, 26, -63, 44, 1, -7, -28, 42, -14, -17, -74, -7, 55, -42, -6, 56, 20, -2, 46, 53, -1, 64, 24, 38, 36, 32, 26, -20, -40, -45, 44, 3, 38, 66, 16, 14, 35, -29, 74, -5, -37, 0, -47, 89, -16, 9, -62, -24, -13, -15, -28, 29, 16, 4, -14, -4, 8, 2, -24, -2, 4, -26, 13, 8, -24, -5, 46, 1, 40, 56, 72, -79, 28, 93, -15, -28, -53, 76, 4, -18, 35, -34, 13, 21, -20, 5, -8, 18, 50, 4, -29, -5, 17, 42, -26, 0, -21, 5, 36, 33, 70, -18, -9, 9, -25, 33, 36, -34, 34, -20, 34, -8, 22, -12, -24, -18, -22, -14, 56, 18, 5, 3, 9, -32, 51, 18, 33, 11, -8, 26, -40, -33, 59, 16, -17, -44, -31, -23, 13, -1, -23, 21, 48, 14, -33, -34, -43, -51, 51, -8, 49, 13, -18, 90, 18, -84, 29, -55, -40, -27, 37, -32, 29, 64, -7, 23, 33, -61, -8, 4, -37, -17, 29, 4, 9, 11, -35, 4, 12, 3, 7, 40, 21, -2, 6, -42, 16, -8, 10, 1, 13, 4, -27, 20, 76, 33, 1, -42, -20, -65, 2, 5, -22, 27, 66, -11, -77, 11, -18, -59, 0, 11, -10, 20, 62, -13, -28, -53, 28, -20, 8, 70, 6, -31, -18, 39, 9, 33, 1, 4, -20, -30, 18, -127, -47, -21, -113, 50, 12, 51, 8, 67, -18, -38, 1, -3, -51, 20, -2, 28, -29, 6, 18, -51, 0, 1, -32, -37, 42, 62, 8, 19, -41, -44, 52, -43, -51, -48, -26, -87, -13, 61, -2, 9, 26, 24, 33, -77, 15, 23, -21, 5, 39, -2, -48, -70, 73, 23, -50, -12, 46, 30, 58, 48, 91, 46, 49, 35, -20, 20, -44, 2, -127, -15, -71, -17, -25, -43, 38, -45, 59, 83, 50, -39, -37, -39, -34, -4, -44, -47, 59, -23, -40, -110, -4, 32, -4, 0, -48, 29, -92, -48, -70, -6, -79, -61, -35, -6, 3, 47, 0, -14, 67, 36, -43, 27, 11, -50, -41, -19, 15, -10, -24, -20, 30, 61, -12, -4, 9, 7, 29, -81, -14, -9, -5, 27, -38, -10, -46, -31, -8, -39, -10, -73, 8, 5, -30, 19, 6, 3, -57, -86, -17, -13, -15, 28, 31, 12, -13, 5, -54, 36, -34, -17, -7, 31, -24, -50, 54, -25, 46, -61, 45, -9, 0, -6, 16, 5, -18, -4, -45, 30, -79, -58, -12, 14, -4, 17, -4, 74, 30, -12, 37, 32, -78, 10, 26, -1, 23, 22, 57, 9, 44, 27, -21, 10, -55, 31, 1, 50, -34, 1, 24, -33, 69, -44, 73, -11, -52, 11, 19, 72, -19, 32, 7, 38, -35, -1, 48, 1, -1, 15, 9, -55, 10, -35, -35, 12, -11, 1, -38, -56, -22, 8, 15, -27, 28, -17, 38, 22, -2, 21, 20, 0, -18, 10, 42, 64, 13, 0, 59, 76, -2, 19, 4, 88, 15, -6, 3, -65, -9, 17, -2, 71, 6, 7, -34, -24, -12, -22, 0, 68, 38, 97, -2, -9, -4, -79, 8, -68, 8, 30, -9, 23, -48, -13, 5, 16, -16, -29, 5, 29, -43, -31, 12, 1, -28, -33, -34, -9, -16, -28, -4, -53, -2, -10, -10, -19, -38, 26, -23, -67, 6, -21, 62, -33, 29, 94, 32, -20, -90, 6, 19, -6, 45, 23, -24, -32, 67, 67, -40, -30, -6, -38, 13, -25, -15, -50, -38, 43, 96, -15, -11, 26, -21, -5, -29, 51, 30, -4, -9, -13, 4, 10, 11, -4, 11, -3, 25, -12, 69, -32, 37, 86, 66, 20, -25, -16, 23, -28, -20, 12, -14, 0, 80, -6, 21, 12, -11, 2, 14, -40, -13, -20, -18, 12, -36, 5, 58, -10, 20, -5, -20, 62, 0, -34, 34, 13, -22, -16, 26, -21, -5, 89, -12, -24, 46, 6, -13, -2, -11, -30, -24, 23, -9, -74, -14, 21, 8, -23, 16, 59, -49, 12, -1, 23, 14, 13, 37, 58, -13, 11, 25, -24, 86, -11, -46, -7, 31, 10, 4, 24, -16, -25, -21, -4, 4, 4, 63, 9, 16, -33, 59, 18, 91, -24, 11, 70, 5, -10, -5, 5, 59, 30, 1, 2, -76, 18, -11, -24, -32, 5, 2, 54, 14, -11, -127, 32, -54, -11, 7, 98, -17, -31, -34, -5, 11, 29, 8, 5, 6, -45, 11, -1, -16, -17, -49, -29, -5, -30, 52, 38, -39, -31, -22, 12, 1, 0, -16, 19, 22, -54, 9, -45, 66, 12, 5, 98, 12, -11, -27, -4, 10, -5, -55, 39, -16, 16, 16, 16, 38, -42, -34, -42, 36, -83, 62, -65, 0, 4, -57, 20, -3, -14, -21, -24, 23, -19, 54, -21, -67, -64, 15, -6, 11, 45, 88, -13, -35, -6, 38, -25, -21, -2, 11, -21, 11, -24, 14, -39, -23, -69, -21, 13, -8, -6, -6, -127, -33, 68, -19, 49, -96, -6, 1, 29, -1, 1, 71, 20, 12, 1, 23, -41, 27, 54, -1, 11, 12, -10, 28, -6, 38, 15, -18, -60, -9, -43, -4, -27, -50, -7, 49, -11, -26, -15, 62, -70, 19, -3, 15, 101, -72, -16, -44, -34, 30, 9, 10, 38, 34, -50, -3, -4, 0, -6, 6, 15, -65, 10, -62, -17, -37, -7, -33, -89, -10, 21, 85, 33, 40, 8, -25, 12, -4, -6, -8, 1, 38, 32, 56, 27, -53, 37, 9, -3, 31, -3, 31, -52, -31, -13, 19, -71, -5, 26, 103, 84, -96, -8, 12, 27, -38, 14, 74, -37, -17, -40, 32, 4, 42, 4, -27, -41, -43, -25, 25, 0, -1, -12, -5, 33, -14, -35, 85, 15, -25, -36, -10, -3, 103, 39, -26, 58, 2, -27, 73, 68, -82, -6, 15, -40, -27, -87, 11, 31, 43, 35, -20, 10, 36, -70, 54, 8, -31, -15, -60, -1, 42, 82, -50, 3, -20, -33, 0, 13, -24, -5, 6, 6, 84, -38, 28, 4, 67, 0, 8, 11, -75, 28, -47, -25, 18, -15, -6, -56, -8, -5, 17, -15, -56, 17, -46, 33, -29, 27, 18, 58, -15, -6, 20, 3, 6, -23, 8, 3, 18, -10, -35, -17, -19, -127, 31, -5, -34, -33, 73, -1, 27, 0, 38, 41, -1, 105, -72, 0, -20, 26, -9, -13, 0, 4, -25, -4, 53, 45, 5, -111, 73, -10, 0, 22, -93, 41, -1, 45, -53, 42, 78, -27, -27, 0, 27, -30, -9, 28, 38, -14, -7, -10, -14, 17, 4, -9, -4, 72, -8, 4, -30, 24, 21, -71, 10, -46, -10, 1, 75, -36, -14, -33, 29, 31, -50, -97, 12, -25, 21, -19, -11, 8, 39, 18, -3, 112, 11, -58, -15, 52, -30, -58, 17, 11, -6, -10, 5, 60, -33, 11, 14, 10, 59, 23, -33, 13, 8, 2, 3, -29, -4, 2, -29, 3, -12, -4, 24, 28, 42, -20, -62, -18, -12, 19, 35, -34, 8, -36, -4, -17, -38, -14, -25, -22, 51, -45, 12, 11, 7, 24, -36, -32, 4, -84, -56, -52, -3, 24, -5, -15, 3, -41, -26, 47, -9, 20, 15, -58, -40, -12, 2, -16, -3, -49, -10, 33, -27, -23, 13, -21, -100, -17, 19, 14, -22, -49, -6, -22, -36, -12, -1, -7, -12, 2, -64, 51, 32, -35, 84, 23, 29, -41, 35, -10, 4, 15, -95, -85, -40, 3, 12, 52, 22, -33, -49, -34, -31, 23, -15, -18, 127, -26, -25, 26, 105, 10, 24, 2, -26, -28, 39, 21, 93, 4, -71, -5, -41, -7, 63, 15, 20, -11, 26, 21, -75, 15, 51, -22, 40, 21, -120, 59, 40, -38, 0, 33, -54, -55, 4, -32, 25, 8, 34, 10, -47, 26, 67, -3, 34, -55, -39, -34, 26, 23, -68, -2, -19, -13, 3, -102, -28, -53, -35, -25, 52, -22, 17, -52, -41, -18, 110, 28, -18, 11, 54, 19, -20, -23, 35, -67, 39, 1, -15, -4, -25, 7, -19, 8, -60, -9, 30, -61, 16, -1, 35, 7, 36, -47, 1, 0, 13, 41, 55, 74, 64, 8, 35, 50, -20, -57, -36, -8, 49, 2, 12, 23, -20, -81, 36, -12, -38, 16, -18, 6, -12, -26, 2, -47, -25, 98, -27, 2, 118, -3, -33, 77, 35, -10, 19, 3, 23, 73, 28, -44, 81, 54, -48, 76, -63, -8, -6, 13, 9, 53, 30, 16, -23, 3, -64, 9, 45, -75, -4, 7, 62, 29, -19, 30, 10, 3, 27, -60, 31, 13, -65, 0, -23, 4, 42, -31, -30, 20, 34, 13, -17, -13, -30, 64, -16, 11, 45, 8, -9, -12, 51, -14, -33, 71, 67, -65, 0, -116, -18, -26, 49, -30, -56, 28, -46, -21, -11, 15, 30, -14, -16, 55, 37, -11, -8, 56, -8, -41, 15, -4, -34, 2, 30, 17, 30, 13, -11, 3, -35, -18, -6, -13, -9, -20, 14, -5, -29, -32, -15, 15, -7, -37, -42, -47, 11, 87, -21, 27, 7, -44, 12, 76, -8, 20, -44, -1, 2, 5, -52, -77, 49, 4, -8, -9, -2, 8, 52, -14, -31, 66, 5, -14, -2, -3, 61, -4, -31, -14, 3, -7, 15, 12, 58, 20, -81, -39, -8, 5, -8, -36, -49, -35, -4, -5, 8, 47, -41, 26, -14, 5, 57, -13, 8, -4, -45, 64, 10, 33, 59, 3, -31, 13, -66, -16, -30, -1, -49, -55, 79, -17, -9, -4, -13, -70, 46, -38, 14, 10, 6, -32, -5, 102, -27, 33, 93, -45, -60, 40, -19, 33, 35, -29, 21, 26, -55, 32, -20, -13, -60, 52, 24, 13, 32, 44, -17, 42, -48, 28, -26, -1, -17, -17, 126, 64, 7, -31, 49, -127, -12, 61, -6, 67, 42, 18, -24, 20, 98, 51, -21, -20, -62, 12, -60, 15, -30, -111, -34, -31, 3, -9, -54, -1, -9, -15, -22, -34, -48, 9, -12, -8, 15, -82, 34, 5, 82, 38, -35, -32, -21, -47, -4, 17, -40, 59, 13, 2, 30, 25, 28, 28, 16, 62, -87, 34, 48, 46, 24, -43, 23, -3, 21, 124, -14, -15, 76, -2, -27, 4, 90, -35, -15, -31, 34, 59, -33, -4, -20, -35, 28, 16, -54, -86, 13, 61, -22, 4, -54, 78, -14, 22, -21, 26, 12, -25, 4, 16, 41, -2, 67, 6, -48, 73, 25, 18, -34, -3, 13, 17, 30, -42, 20, -22, -14, 28, -15, -4, 35, 20, 25, 11, -11, 29, 34, -15, -16, -25, 36, 36, -17, -15, 61, 55, -40, -1, 37, 25, 26, -30, 4, -35, 18, 50, -42, 21, -16, 58, 48, 20, 2, 12, -26, 5, 19, -85, 10, 95, 58, -17, -12, -27, -26, 8, -41, -27, 22, 4, -45, -8, 54, -7, 2, -25, 25, -23, 15, -75, 60, 25, -26, 11, -83, -48, 18, 45, -27, 14, 39, -53, -3, -32, 29, 19, 47, -37, 19, 2, -87, -46, -6, -5, 49, -16, 25, -28, -27, -98, -24, -5, -36, 68, -80, -60, 20, -22, 8, -13, 52, -22, -64, 2, 21, -11, -12, -37, -29, -41, 10, -8, -48, 40, -50, 57, -5, 9, 11, -85, -7, -10, -60, -15, -41, 46, -13, 127, 54, 24, -7, 24, 35, 25, -21, -62, 61, 18, 21, 28, 78, -27, -72, -90, -7, -31, 64, -11, 53, 16, 16, -28, -13, 12, -20, 19, -44, 13, -36, 4, -82, -36, -16, -46, 22, -15, 60, 15, 19, -57, -90, -37, 25, -54, -84, 40, 24, -60, 4, -50, 20, 2, -28, 9, 24, -47, 2, 40, -102, 31, 60, 108, 1, 71, 20, -68, -12, -4, -20, 4, 25, 16, 3, -39, -34, 82, 14, 9, -6, 49, 29, -5, -28, -72, -23, -50, -36, 37, 32, -7, -27, -33, 30, -27, 11, 11, 80, 15, -41, 2, -35, 56, 31, -68, -2, -44, 0, -11, 6, -5, -75, -9, 36, 27, -54, -37, 13, -32, 8, -16, 55, -45, -15, 12, 35, -38, -33, -40, -58, -41, 34, -26, -1, -60, -11, 3, 3, 34, 41, -5, -14, -22, 3, 40, -22, 7, -30, 9, 0, -48, -36, -5, -37, -7, 29, -45, 64, -96, -19, -91, -32, 87, 1, -15, 59, 11, -21, -17, -18, 50, 36, 27, -3, -8, 16, -10, -21, 36, -15, 127, -38, -15, 43, -119, -13, -21, -6, -68, -44, 19, 29, 14, 18, 37, -70, -14, -32, 24, 27, 4, -13, -6, 3, 43, -19, -2, 25, 24, -11, 15, -22, -1, 32, -7, -3, 36, 4, 84, -20, 37, -13, -9, 42, -26, 6, -1, -15, 8, 34, -20, 39, -1, -7, -65, -20, 126, -28, 58, 2, -3, 45, -48, -11, -59, 18, -20, 7, 24, 57, -40, -16, -91, 47, -32, -20, -47, -19, 3, 33, 20, -38, 34, -7, -22, -20, 33, -81, -33, 17, -6, -94, 13, -27, -39, -7, -48, 12, -46, 21, 47, 3, 26, -8, -3, 40, 44, -10, 31, 61, 17, -58, -10, -4, 32, 41, 97, 36, -18, 23, -72, 40, 43, -41, 20, -29, -48, -30, 21, 1, -34, 1, -16, 61, -8, 14, 3, 16, 4, -8, -44, 1, -7, -62, -10, 47, -28, 14, 16, 3, 2, 18, -1, 15, 32, 36, 17, 29, -24, 65, 4, -7, -11, 5, 13, -9, 17, 15, 56, 11, 60, -8, -1, 18, 38, 21, 26, -10, -63, 62, 45, 1, -34, 7, -55, -27, 49, -9, 7, 7, 33, 15, 12, 37, 14, -58, 24, 26, 51, -11, 5, 6, -5, 0, -37, 15, 0, 4, -4, -20, -38, -8, 38, 16, 5, 31, -20, -9, -10, 2, -41, 17, 10, 7, -15, 4, -20, -4, -14, 31, -10, -99, -29, 27, 18, 23, -33, -56, 18, -12, 12, 6, -6, -52, -20, -43, -9, 26, -5, -40, -13, -1, 5, 45, -42, 1, -37, -8, 5, -18, -2, 54, 15, 4, 37, -20, -11, 22, 21, -22, 17, 11, 21, 7, -127, 2, 40, -53, 18, 10, 23, -6, -15, 24, -1, 43, 17, 80, -65, 23, 7, -11, -79, -1, 46, 34, 7, -74, -21, 29, 5, -72, -43, 15, 7, 34, 20, -28, -68, 49, 54, 5, -3, -7, 8, 34, 14, 10, 3, 1, 72, -6, -16, -4, -51, 44, 13, 17, 4, -103, -11, 15, -29, -23, 14, 21, -36, 15, -12, -94, 13, -4, 6, -7, 0, 50, 9, -47, -29, -17, 21, 53, -31, -21, -33, 65, -17, -6, 18, -30, 6, 8, 34, 65, 26, 10, -13, -63, 29, 0, 6, 2, -3, -19, -19, -20, -10, 17, 40, -14, 4, -18, -13, 6, -2, -9, 39, -19, -22, 3, 37, 23, -43, -8, -15, -92, 32, 88, 23, 9, 50, 15, 47, 49, -35, 25, 19, -41, 16, -19, 52, -76, 39, 33, -15, 24, 93, -6, 63, 93, -12, 61, 14, -26, 21, 43, -51, -46, 11, 8, -7, -17, 34, 6, -95, -9, 26, -25, -3, -15, 36, -3, 19, -2, 44, -34, 30, -44, -57, -27, 25, 20, -2, -21, -43, -14, 67, 23, 16, 32, -105, -31, -18, 44, 49, -92, 7, 33, -7, 125, -24, 71, 14, 14, 18, 11, -20, 5, 37, -42, -19, 2, 1, -11, -9, 45, 5, 32, 29, -62, 20, 47, 22, -38, -5, 21, -69, -1, 33, -31, -6, -34, -33, 104, 73, -42, 37, -14, -41, 5, 9, 17, -11, 76, 59, 38, 6, 41, -45, 10, 7, -71, -42, 6, 34, 3, 41, 40, -37, 0, 70, 41, -24, 41, -64, -15, 1, -3, 41, -31, 19, -42, -10, -11, 20, 42, 4, 51, 22, -16, 11, 11, 10, 94, -35, 9, -17, 40, -17, 21, 79, -36, 2, -11, -29, 35, 56, -13, -4, -67, -73, 26, 23, -27, 3, -102, -15, 35, -127, 23, -24, -80, -47, 11, -40, -36, -31, -85, -22, 17, -46, 36, -18, 8, 52, -17, -73, -5, -10, -44, 54, 23, 20, 53, 22, 25, -8, 60, -27, 27, 56, -3, 19, -68, 42, 6, -5, -37, -39, 23, 9, 17, -61, 14, 6, -3, 127, -6, -37, -44, 73, 73, 5, -15, 9, 27, -43, 24, -54, 37, 41, -7, -26, -38, 65, 9, 19, -7, 22, -108, 13, -31, -58, -4, -13, -5, -7, -41, -61, 7, -6, -11, 16, -16, -12, -24, 15, 2, -22, -5, 25, 0, -7, 22, -21, 64, -9, 22, 11, -11, -39, -2, 54, 6, -36, -64, 4, -14, 33, 20, 28, -43, 9, -3, 2, -49, -53, 30, -18, 77, -12, 0, -15, -43, -28, -18, -47, 75, 47, -2, -10, -13, 60, -109, 66, -9, -2, 2, -33, -30, 4, -48, -5, -44, 4, -4, 34, -26, 1, -20, 5, -7, -102, -34, 0, 3, -54, 42, 24, -15, -16, 55, 4, 76, -21, -22, -17, -12, -14, -6, -12, -26, 28, -13, 18, 71, 3, -69, -15, 59, -18, -20, -13, 4, 69, -34, -37, -1, -5, 14, 20, 12, -11, 3, -34, 21, -12, 32, -4, 38, 53, -22, 36, 80, -10, 31, -80, -15, 24, 7, 9, 127, 16, 23, -11, 21, -24, -42, 8, 60, -21, 2, 3, -77, 41, 27, -48, -10, 75, -15, -12, 56, -5, -22, -20, -7, 29, -36, -55, -10, 30, 19, -15, 55, 14, 88, -12, -4, -29, 9, 37, 8, -8, 23, -25, -8, -11, 12, -22, 74, 13, -11, -71, -3, -36, 11, 0, 9, 11, 33, 69, 66, -58, -10, 30, 43, -18, -13, -60, 54, -28, 13, -73, 9, 30, -35, 7, -7, -21, -3, -6, -6, 53, -20, 16, 22, -68, 44, 21, 19, -34, 56, -4, -6, -17, 6, 51, 13, -27, -13, -14, 4, 59, -1, -55, -57, 46, -35, 48, -36, -11, -17, 15, 9, -32, 12, -20, 10, -9, 36, -19, 7, 51, 40, -32, 0, 37, 59, -34, -75, -62, 52, -10, -91, 5, 91, 15, -56, -1, -1, -27, -18, 9, 21, 19, 38, 4, -54, -3, -7, 1, 66, -26, -65, -21, -22, 32, -4, -31, -19, -33, 49, 8, -31, -4, 20, -24, 5, 10, -3, 1, 53, 14, -64, 23, -1, 3, -44, 23, -6, 7, 45, -2, 40, -9, 31, 33, 28, -23, -48, 15, 19, -12, -54, 1, -18, 26, -8, -17, -5, -1, -29, -22, 20, 8, 2, 75, 21, -52, 77, -34, -28, -18, 78, -12, 13, -56, 15, -20, 44, 0, 26, -7, 8, -20, 5, -18, -47, -127, -27, 27, 43, 13, -44, -12, -62, 50, 52, -18, -14, -1, -39, 30, -2, 8, 11, 1, -47, -13, -23, 0, 60, -13, 65, -11, 24, 33, 58, -25, -17, -22, 48, -10, 4, 1, 43, -51, -74, -36, -12, -6, 29, -20, 6, 40, 107, -4, 6, 5, -29, 38, -16, 5, 62, 20, 22, -68, -11, 19, 96, 31, 35, -43, 1, -86, 8, 24, -17, -4, -19, 28, -7, 53, 73, -16, 6, 14, -10, -11, 16, 17, -12, -65, 2, 14, -41, 3, 9, -18, -17, 30, -3, 65, -26, -24, 0, -21, 18, -5, -60, 0, -11, 53, -11, -11, -10, 5, -5, -34, -7, 89, 6, 20, 12, 15, 48, -27, 4, 13, -25, -46, 6, -26, -43, 64, 21, -20, 12, -3, -5, -42, 31, -27, -52, 35, -6, 37, 18, -34, 48, 14, 44, 21, -31, 49, 20, 27, 49, -5, -12, -17, -6, -10, -10, -23, -42, 25, -24, -35, 18, 27, 2, 10, 11, 11, 37, 52, 18, -18, 20, -1, 19, -5, -18, -11, -21, -18, -38, -31, -22, -13, 25, -24, 13, -32, 28, -17, 9, -40, 45, 127, 6, -12, 62, 26, 8, 72, -19, 45, 33, -17, -55, -23, -17, 11, 18, -25, 17, -25, -36, 0, -9, 45, -21, 10, 19, -27, -23, 31, 1, 21, -35, -6, -50, 20, -21, -1, 71, 5, 27, -28, 1, -15, -25, -65, 61, 37, -9, -15, 33, 18, -19, 22, -26, -28, 12, 3, 22, 47, -15, 36, 29, 6, -62, 4, 5, 17, 9, -59, -8, 5, 22, -37, -2, 71, 76, -58, -72, 32, 43, 28, 38, -33, 61, 3, 34, -22, -8, 22, 7, -7, 7, 20, 5, 19, 8, -98, -17, 21, 50, 8, -2, -77, -19, 4, -39, 24, -27, 16, 18, 18, 2, 4, 26, -6, 66, -1, 3, 7, -27, 0, 13, -9, -51, 22, -107, 48, -11, 53, 105, -34, -1, -127, 18, 38, -2, 8, -35, 2, -29, -80, -92, 50, 47, 19, 28, 44, 57, 13, -27, 47, 57, -108, -78, -16, 58, 39, 7, 24, -83, -10, -1, -15, 9, 16, 45, 2, -67, -15, -43, 24, 66, -7, 28, -12, -19, -57, -9, 1, 37, 3, -12, 20, -3, 108, 17, -53, 43, -6, 35, 73, -40, -80, -95, 23, 28, 15, 22, 28, 11, -12, 37, 26, 13, 24, -33, -27, 47, -23, -87, 36, -75, 58, 68, -34, 43, 7, -89, 58, 33, -11, -28, -55, -51, -63, -79, 50, -13, -17, 23, 10, -45, -39, -21, -10, 19, 4, 60, -56, 6, 7, 84, -54, -26, -31, 22, 21, 17, -49, -13, -52, 32, 32, 65, 100, 56, -21, -20, 36, -40, -24, 39, 21, -31, 12, -85, -56, -3, -87, 99, -5, 17, 16, -84, -87, -16, 67, -9, -83, -4, 5, 101, -20, 26, -48, 0, 17, -5, -79, 38, -63, 1, -1, 32, 30, 21, 41, 20, 2, 30, -11, -20, -84, -25, -86, 37, 25, 78, -9, 41, 49, 13, -46, -9, -22, 39, -81, -8, -28, -32, -24, 42, -44, -19, 32, -10, 11, 50, 11, 46, 31, -30, 7, -51, -22, 66, 60, -6, 43, -19, 28, 6, -18, 4, -51, -3, -6, -24, -33, -53, -9, 53, 7, -14, 41, 10, 5, 6, 9, -28, -53, -36, -20, 107, -49, -8, 19, 11, 19, -19, -15, -15, 56, -5, -1, 28, 29, -5, -89, 28, -54, -22, 17, -13, -19, -5, 33, 33, 56, 42, 16, -74, -49, -11, -5, 45, -22, 24, 66, 16, -8, -15, 19, 30, 21, 26, 81, -19, 8, -22, 17, 87, 35, -5, -14, -4, -22, -17, 44, -46, 22, -94, -105, -45, 60, 17, -66, 4, -71, -7, 19, 21, -34, -13, -38, -35, 32, -20, -3, 15, -13, 19, -18, 8, 7, 50, 7, -11, 40, -42, -22, -25, -75, 17, 32, -1, -33, -42, -28, -4, -4, -97, -14, -15, -32, -19, -54, -3, 7, 69, -29, -60, -65, -127, -10, -39, -22, 36, 29, -4, 22, 31, 10, -12, -18, -15, 26, -12, 19, 48, 42, 58, -31, 55, -40, -41, -33, -20, -66, -26, 14, 20, -37, -18, 22, -38, 28, -1, -1, -54, 13, 25, -41, 46, -23, 64, 28, 21, -90, -24, 54, -28, -19, 35, -7, 20, -58, -41, 16, -83, -47, -23, 63, 10, 23, 69, 3, 18, -104, -12, 31, 8, 53, -9, 19, 46, -12, 22, 71, 11, 81, 25, 41, 64, -32, -88, -54, 60, 34, 6, 11, 31, 37, 26, -42, 11, -76, 28, 89, 15, 18, -49, -67, -23, -71, -4, -6, -7, 81, -1, -8, 28, 13, 58, -6, 5, 84, 48, 18, -43, 43, -11, -15, -13, 36, 2, 49, 3, -25, 19, 23, -50, -56, 8, 5, 19, -6, 16, -4, 12, -53, 1, -4, 68, 15, 13, 25, 50, 18, -25, 18, -1, -13, -29, 58, -47, -20, -21, 26, 55, 3, -14, -16, 21, 2, -10, -17, 17, 16, 39, 90, -2, 5, -47, 5, 6, -4, -18, -3, 5, 23, -1, 28, 17, -72, 3, 31, 15, -19, 19, -14, -9, -17, 20, 1, -31, -54, 0, 25, -13, -4, 0, 11, -7, -22, 29, 3, 26, 0, -9, 14, -6, 50, 1, -6, 7, -53, 6, 5, 20, 24, 25, 21, -7, -37, 11, -57, 20, -25, 14, -8, -16, 1, 40, 16, 16, 24, -51, 21, -6, -16, 27, 5, -36, -2, -17, 20, 7, 13, 22, -34, -32, 1, 57, -9, 12, -19, 1, 36, -18, -33, 23, 23, -26, -6, -20, -20, 6, 90, 9, 13, 1, -6, -47, 109, 18, 16, -14, -37, -7, 44, -26, 26, 14, 11, 0, -1, 48, -51, 10, 3, -50, -2, -2, 100, -67, 1, -35, -1, -11, 7, -11, 33, 36, 4, -26, 9, -39, 18, 9, 6, 21, 1, 6, 10, 10, 44, 8, -22, 4, 52, 0, 12, -25, -65, -24, 13, 10, -9, -25, -21, -1, 2, -28, -4, 11, -12, -13, 10, -17, -8, 9, 14, -13, 12, -2, 127, -14, 7, 10, 50, 5, -93, -35, -7, 30, -3, -6, 20, -9, -13, -6, 15, -10, 22, -12, 8, -19, 23, 4, -10, -8, 13, -30, 40, -37, -27, 42, -15, -27, -23, 21, 25, -12, -80, -15, -60, 8, -6, -40, -27, -10, 12, -5, -15, -23, -9, -20, 7, 29, -14, -81, -30, -26, 2, -17, -9, -29, 56, -13, 8, 28, -84, -10, 11, 9, 2, -16, -49, 28, 19, 3, -18, -8, -56, 37, -5, 20, 7, -82, -29, -127, 44, -8, 63, -5, 4, 15, -42, -73, -20, 53, -24, 57, 13, -32, -12, 68, 36, 9, 65, -18, -17, -39, -28, -44, 1, 48, 40, -23, 108, 21, 10, -19, 11, 44, 26, -31, 25, 70, 44, -7, -19, 30, -3, -31, -56, -6, 28, 35, -4, -40, 55, 20, 1, 3, 26, -6, 0, 23, -8, -42, 10, -83, 3, -74, 5, 47, 24, -122, 3, 59, -41, 50, 43, 8, -43, 36, -6, -79, 23, -24, 35, -7, 45, -31, -44, -21, 123, -23, -48, 42, -59, 53, -6, 17, -22, -8, -67, -20, -78, -26, 24, -74, 12, -2, 62, 36, 56, -87, -24, -27, 43, -17, 18, 52, 39, 63, 53, 18, -67, 50, -64, -83, -87, 30, -77, -14, -12, 35, 59, 61, 30, -30, 79, -13, 90, 55, -5, 20, -49, -53, 31, 37, -29, -55, 12, -12, -38, 16, 31, -11, -64, -20, -35, -38, 22, -13, 46, -22, 27, 7, -7, 48, 63, 64, -21, 33, -32, 39, 48, 61, -3, 26, -17, -11, 48, -68, -28, 80, 9, -24, -16, 0, 8, 10, -10, 3, 17, -2, 15, 13, 13, -10, -12, 17, 30, 10, 43, 13, 16, 14, -27, -3, 24, -18, 24, 7, -7, 25, 2, -6, -9, -25, -14, -13, 6, 46, -12, -3, 84, -19, -56, -27, 21, 15, -20, 15, -42, -1, -22, 4, 17, 21, 22, 10, 4, 31, 3, 10, -30, -11, -30, 24, -16, 24, -7, -2, -7, 7, 13, 8, 0, -19, -19, -16, 23, -26, 7, 1, 18, 2, 19, 11, 32, -27, 1, 8, -3, 8, 1, 22, 7, 14, 0, -29, 28, 6, 11, 21, -36, 7, 9, -20, 16, 0, 14, -43, 3, 127, -45, 0, 17, -30, 11, 6, 8, 4, 33, 7, 6, -18, 3, 20, 29, 6, -20, 1, 38, -6, 5, 74, -6, -23, -4, -25, 8, -6, 22, 9, 4, -2, 31, 16, -40, 7, 2, 18, -18, 14, 9, -5, -3, 1, 13, 11, 12, 75, -6, -6, 3, 3, 5, -3, 10, -7, -25, -2, 16, -52, -16, -10, 25, -33, 5, 40, 22, 10, -4, 30, -26, 25, -1, -2, -7, -17, 14, 13, 12, 28, -18, 3, 5, -9, -5, -78, 13, 52, -8, -5, -10, 6, -5, 43, 11, -4, 4, 17, -9, -1, 2, 1, -2, 9, 5, 13, 5, 67, 39, 3, -14, -21, -88, -36, -2, 1, -22, 12, 5, -46, -3, 3, 11, 2, 14, 18, 21, -48, -20, -5, -5, 7, -4, -28, -47, 21, 32, 9, 36, -6, -60, 6, -41, -9, -14, 9, 43, 37, 20, 3, 56, 49, -35, 8, -12, 12, 41, -39, -33, 1, 5, 63, -13, -9, 23, -63, -15, -6, 7, -73, -17, -66, -42, -54, 12, -101, -43, 24, -58, -85, 6, -31, -52, -37, -93, 127, -3, -5, -3, -18, -23, 67, 13, 17, -42, -30, 52, -34, 14, 34, 30, -41, 31, 14, -10, -7, 53, 20, -4, 17, -42, -21, 3, -31, -14, 10, -36, 43, 30, 49, 32, -5, 60, 36, -28, 7, -7, 45, 48, -13, -10, 29, 16, 64, 29, 16, 27, 21, -8, 2, 10, -32, -17, 43, -10, -71, -33, 7, -55, 63, -53, 16, 46, 27, 7, -15, 61, -24, 9, -3, -92, -60, 19, -1, 87, 46, 78, 14, 30, -39, -22, -3, 21, -23, 37, 48, -4, -13, 10, 18, 68, -49, 50, -5, 41, 7, 27, -36, -9, -10, -22, 12, 29, -16, 56, 6, 100, -50, 9, 39, 12, -51, -7, -34, -10, -26, 20, -94, 5, 8, -38, -11, -29, -12, -21, 26, 67, 21, -54, 35, 72, -93, -1, -31, -48, -16, 85, -60, 37, -100, -66, -10, 1, 56, 24, -39, 7, 23, -104, -40, -32, 40, 25, 13, 73, 28, 36, 60, 7, 58, 72, -30, 24, -36, 99, -45, -19, 9, -41, -35, -34, -43, -2, -66, 25, -37, -5, 41, -1, -26, -13, 16, -18, -60, 10, 4, 7, -1, -10, -17, 7, 18, -20, 8, 16, -20, 7, 17, -52, -21, -1, -10, -44, 16, 3, -40, 3, 9, 42, 29, -19, 26, -15, 27, -2, 9, -5, -2, -21, -1, 11, -3, 35, 11, 13, -27, -2, 34, 3, 27, -10, 5, 38, 10, -37, 46, 34, -20, -6, 13, 30, 16, 27, 2, -7, 33, -7, 11, 7, 7, -3, -9, -3, 9, 31, 19, -1, 45, 0, -55, -28, 5, 4, -5, 1, -74, -24, -11, 30, 12, -8, -14, 20, -5, -4, 1, -25, -15, -2, 9, 8, 61, -12, 34, -10, -7, -6, -9, 57, -13, -87, 16, 1, 12, 34, 8, 15, 7, -49, 0, 48, -8, 19, -8, -28, -2, 34, 21, 5, 10, -39, -33, 21, 51, 70, 13, 14, 3, 14, 15, -8, 2, -43, -1, -78, -8, 11, -32, 2, 10, 0, -17, 18, 6, -18, 27, -9, 82, 1, 12, -3, -7, -107, -5, 16, -26, 65, 25, -9, -42, 9, 28, -8, 34, 6, 16, 49, -21, 28, -49, 85, 16, -26, 35, -26, -30, -27, -8, -18, 69, 8, -12, 24, -9, 6, -44, 43, 25, -4, 1, -45, -1, -24, -40, -20, -34, 26, 17, -28, -9, -5, -7, 8, 3, 10, -33, 2, 127, 9, -12, 26, -35, 13, 30, 37, 17, 18, 11, 7, -14, 15, -19, -34, 3, 5, 50, -35, 102, 16, 12, 20, -5, 1, 5, 56, -1, -29, -19, -2, -3, 27, -52, 67, 5, -18, 0, -70, -9, -47, -41, -46, -47, -54, 9, 30, -52, -5, 3, -24, -54, 5, 32, -31, 56, 44, -31, 16, -12, 26, -73, 6, -11, -26, -22, -18, -127, -20, 57, -45, 29, -15, 38, -14, -8, 8, -34, 39, 21, 15, 5, 7, -48, 1, -23, -49, 13, 41, -50, 92, 15, 11, -67, -57, 34, -20, -112, -5, 56, -30, 26, 29, -8, 27, -8, -16, -58, -39, 44, -2, -58, -11, -24, 36, 3, -35, 9, -50, -54, -14, -127, -29, -20, -29, -23, -62, 0, 17, 13, 43, 21, 88, -31, -18, 22, 4, -18, 29, -1, -24, 0, 10, -106, -34, -2, -14, 8, -63, 33, -22, -14, -45, 32, 11, 43, -42, -17, 33, 5, 11, 38, -29, -2, 44, -53, 35, -56, 92, -89, 15, 9, -19, 25, 10, 107, -54, 41, 3, 2, 3, -7, -44, -21, -7, -45, 40, -3, -16, 12, 103, 8, -43, -56, 33, 15, 0, -86, -67, -2, -29, -48, -17, 93, -9, -44, -6, 27, 14, 9, 13, 18, 11, -41, -35, -69, 6, 10, 6, 0, 94, -20, -2, 3, -33, 15, 61, -83, 13, 4, 0, -4, 2, -22, -6, -48, -7, -46, 69, -11, -91, 6, 0, 9, -13, 57, -35, -23, -11, -36, 14, 11, -10, 31, 22, 5, -4, 13, 10, 42, 9, -28, -32, -33, -37, 19, 19, -37, 9, 10, -64, 4, -14, -21, -8, -10, 16, 13, -61, 34, 11, 9, -16, 0, -12, 14, 45, 45, -58, -3, -17, 1, -28, -9, -11, 6, 12, -25, -44, -27, 7, -5, -22, 2, -3, 1, -37, 64, 22, 88, 19, -34, -3, -51, 22, -30, 46, 40, 6, -27, 39, 5, -11, -1, -7, -3, -8, -6, 27, 18, 1, 45, -58, 17, -5, -16, -37, -5, 17, 14, -32, 8, 9, -42, 58, -14, -10, 49, -16, -18, -69, -8, -54, -59, -3, -65, -6, -69, 11, -13, -18, 110, 1, -22, -10, 10, 0, -3, 27, -4, -28, 40, 59, 44, -44, -15, -20, -20, 44, -41, 11, 9, 10, -27, 12, -15, 22, 9, -19, 7, -21, 10, -17, -5, 17, -86, 93, -5, -23, 1, 19, -2, -9, -44, -6, 44, 0, -14, -46, 1, -2, 7, 38, -29, 28, -92, -15, 31, -62, 9, -13, 42, -16, -20, -10, -23, 23, 29, 13, -10, -21, -25, 16, -12, 6, -6, -28, 4, -14, -40, -55, 11, -3, -22, -9, 32, -14, -9, -16, -53, 43, -45, -7, -30, -21, 14, -1, -51, 52, 12, -41, -112, -8, 18, -10, -19, -7, 53, 49, -55, -14, -2, -9, -36, 56, 15, -23, 0, -5, -2, 3, 19, -22, -16, 16, 4, 14, -35, -17, -23, -21, 127, -30, -76, 35, -18, -76, -10, -48, -4, 13, -16, -23, 12, 17, 41, 6, 73, 8, -78, 9, -8, -29, 17, -15, 25, 17, 8, 16, 35, -26, -42, 11, 19, 5, -9, 42, -50, 15, 58, 2, -1, -3, -44, 40, 66, -38, -41, 24, 27, 19, 69, -76, -44, -62, -24, 39, 8, 15, -11, -43, 12, -34, -3, -68, 51, 109, -11, 66, 38, -13, -3, 23, -20, -64, -9, -71, -80, -38, -16, 51, 18, 8, 14, 14, -19, 12, -21, 37, -34, 5, -13, 46, -7, 28, 16, 34, 37, 52, -52, -28, 22, 7, 94, -40, 15, 37, -34, -13, 7, 23, -1, -44, -5, 32, -8, 34, 41, -3, 107, 14, -7, -26, 61, 39, -61, 1, 50, 8, 23, 26, 0, 127, -10, 16, 19, 40, 23, -38, 3, -36, -14, 28, 98, -26, -41, -78, -19, 10, -20, 60, 57, 34, -18, 21, 21, -36, 17, -1, 21, -54, 14, 21, 3, 17, -5, -5, -16, 11, -59, -77, -7, 22, -50, -62, 9, -120, -47, -46, -31, -15, 43, -8, -34, 16, 84, 9, 49, 3, 27, 33, -31, -74, -34, 12, -27, 42, 53, 9, 5, -10, -20, 53, 10, 20, 1, 10, 42, -50, -35, 16, 0, -8, 28, 32, 62, 16, -18, 1, -9, -28, 2, 16, 19, -10, 1, 22, -3, -9, -9, -48, -11, -7, -11, 94, 42, -12, -71, -40, 18, -14, -63, -14, -12, -24, 38, -4, 57, 41, -28, -5, -8, -8, 28, 25, -49, 23, 28, 27, -31, -14, -24, 18, 49, 21, 71, 4, 0, 42, 10, -4, -4, -5, 17, -79, 7, 19, -30, -7, 0, 37, 41, -15, 5, 16, 54, -1, -3, 21, 31, -4, 8, 24, 19, 124, 26, 23, 1, 31, 45, 50, 65, 85, 71, 17, 16, 19, 12, 1, -51, -4, 32, 21, -28, 13, 6, 32, 14, -14, 7, 11, 46, -24, 23, 2, 20, 26, -7, 7, 24, 37, 17, -5, -1, 18, 27, 20, -24, -4, -36, 31, -15, -42, 24, -18, -24, 12, 32, 63, -3, 58, 1, 26, -2, 18, 46, 25, 17, 21, 40, -7, -51, 44, 1, 3, 15, 44, -2, 13, -14, -4, -18, 33, -22, -3, -43, -24, 27, 35, 3, 11, 9, 18, -3, 49, 49, 47, -37, -35, 23, -26, -8, 16, -12, 41, 19, 30, -8, 10, 18, -17, -35, -2, -17, 15, -8, -23, 24, -22, -22, -8, 23, 22, 30, 11, 0, 11, 10, 59, 24, -14, 0, 35, -12, 33, -48, 1, 11, 25, -8, 4, 3, 52, 70, 50, 48, 27, -2, -36, -12, -33, -5, -44, 6, 22, 1, 10, 127, 9, -48, 25, 8, -23, -24, 30, 19, -16, 8, 60, -11, 49, -36, -18, -6, 41, 17, -7, -35, 31, 21, 5, 96, -15, 41, -5, -88, -22, 12, 58, 15, -11, -62, 33, -28, -36, -12, -13, 43, -33, 27, 20, 31, 47, 22, 1, -36, -2, 46, -3, -30, -35, 4, -96, -13, 16, -7, 22, -65, 25, 35, -42, 8, 5, 5, -25, -7, 26, 64, -35, -57, 43, -30, -46, -20, 85, -29, -54, -10, -47, -13, 17, -25, 19, 38, -23, -25, -28, -48, -24, 65, -44, -38, -44, -51, -34, -3, -7, -7, 2, -38, 3, -25, 24, -41, 9, -7, -73, 46, -34, -30, -23, -39, -31, 4, 12, 43, 44, 4, 14, 5, -15, -10, 75, -16, 25, -12, -10, -27, -50, -36, 24, 16, 44, 47, 25, -34, -44, 7, 7, 13, 24, -49, 8, 59, 17, 10, 18, 15, 62, -18, 25, 66, -23, -6, 6, 2, -10, 11, -4, -23, 45, 44, -14, -62, 10, -32, 17, -15, -47, -4, -1, -13, -31, -49, 62, -40, -108, 4, 11, -6, 13, -37, 17, -18, 37, -31, -36, 17, -19, -31, -21, -31, 31, 7, 59, -1, -1, -52, -29, -24, -62, -2, -30, -68, -30, -35, -16, -127, -12, -3, -31, 22, 23, 11, 3, 24, -10, -63, 16, -67, 51, -14, 56, 22, 26, -29, 25, 59, 44, -18, -13, -36, 33, 2, -16, 7, -4, -32, 4, -24, 7, 57, -17, -24, 5, 15, -15, -1, 54, -23, -16, 56, -22, 29, 60, 39, 21, -56, 63, -25, 51, 27, 52, 19, -38, -29, -12, -46, -31, 14, 11, 68, -13, -19, -3, -2, -36, -1, 35, 17, 53, -5, 5, 13, -22, -41, 34, -40, -19, 9, 2, 8, 6, -31, -4, -12, 14, 0, 71, -26, 34, 14, 114, 109, 25, 0, 3, 31, 5, -5, 40, 18, -3, 20, 23, -12, -17, 13, -17, -28, -14, 29, 3, -2, 30, 22, -1, -57, -21, 55, 17, -12, 6, 33, -36, -17, 5, -5, -50, -15, 17, 1, -21, 9, 4, 19, 34, -25, -12, -18, -45, 23, 5, -28, 111, 21, 14, 47, -7, 2, -11, -34, -14, -14, -4, -23, -15, -23, 26, -20, -49, 123, 6, -29, 64, 4, 14, 18, 40, 125, 37, -63, -8, -4, 80, 87, 112, -25, -3, -9, 1, 15, -19, 18, -29, 27, -35, -21, 35, -2, -31, -3, -18, 25, 24, 14, 33, -12, 53, 41, 13, 2, -8, 5, -13, -24, 22, -9, 5, -27, 10, -22, -2, -3, -9, -11, -4, 123, 11, 58, -23, 1, -26, 14, -6, 8, 127, 50, 7, -5, -1, 2, -20, 53, 19, -23, 27, 48, -6, -20, 5, -51, 60, -5, -33, -36, -9, -6, 54, 7, -48, 38, -6, -73, -16, 3, -22, -14, -6, 8, 16, -7, 9, -10, -9, 40, -20, -17, -10, 41, -59, -4, 2, -61, -1, 7, 35, 1, -24, 9, 30, -19, -11, -40, 3, 84, -35, -20, -31, -1, 49, 33, -42, -14, -12, 48, -3, 16, 0, 3, -2, 16, -32, 26, 25, -9, 11, -7, -8, -34, -5, -8, -3, 63, 33, -97};

float bias_raw[40]={0.049277037382125854, 2.35211443901062, 0.02653031051158905, 1.4742283821105957, -0.5353764295578003, 3.0161919593811035, 0.1642170250415802, 0.49790701270103455, -0.46246474981307983, -2.6883251667022705, 1.2114373445510864, 1.320859670639038, 0.49988746643066406, -0.4576125144958496, 0.33462536334991455, -2.1160244941711426, 1.2292046546936035, 0.8743401765823364, -1.6769397258758545, 0.4390380084514618, -1.316502332687378, 1.7834322452545166, 0.5155450105667114, 2.131397008895874, -5.296634674072266, 0.9354028701782227, 0.6885842680931091, 0.46311378479003906, -1.8100215196609497, -0.12493062019348145, 0.3935861587524414, 0.008527040481567383, 0.4799034595489502, -0.5655108690261841, 0.34335458278656006, -1.3752316236495972, 0.8314380645751953, -0.5400923490524292, -1.2556636333465576, 0.784142017364502};

int8_t* filter_tensor_data=filter_raw;
float* bias_tensor_data=bias_raw;

bool has_conv_bias=true;
const int stride_width=1;
const int stride_height=1;
const TfLiteFusedActivation activation=kTfLiteActNone;
const int dilation_width_factor=1;
const int dilation_height_factor=1;
const int filter_dims_size=4;
const int32_t filter_dims_raw[4]={40,1,1,240};
const int bias_dims_size=1;
const int32_t bias_dims_raw[1]={40};
const TfLitePadding paddings=kTfLitePaddingSame;
const TfLiteType filter_type=kTfLiteInt8;
const TfLiteType bias_type=kTfLiteFloat32;
const float scale_filter=0.0;
const int32_t zero_point_filter=0;
const float scale_bias=0.0;
const int32_t zero_point_bias=0;
// const float scales_filter=;
// const int32_t zero_points_filter=;
// const float scales_bias=;
// const int32_t zero_points_bias=;

struct OpData {
  // IDs are the arbitrary identifiers used by TF Lite to identify and access
  // memory buffers.
  int im2col_id = kTensorNotAllocated;
  int hwcn_weights_id = kTensorNotAllocated;
  int input_quantized_id = kTensorNotAllocated;
  int scaling_factors_id = kTensorNotAllocated;
  int input_offset_id = kTensorNotAllocated;
  int accum_scratch_id = kTensorNotAllocated;
  // Row sums are used to cache filter sums for hybrid zero-point calculations.
  int row_sums_id = kTensorNotAllocated;

  TfLitePaddingValues padding;
  // The scaling factor from input to output (aka the 'real multiplier') can
  // be represented as a fixed point multiplier plus a left shift.
  int32_t output_multiplier;
  int output_shift;

  // Per channel output multiplier and shift.
  std::vector<int32_t> per_channel_output_multiplier;
  std::vector<int> per_channel_output_shift;

  // The range of the fused activation layer. For example for kNone and
  // uint8_t these would be 0 and 255.
  int32_t output_activation_min;
  int32_t output_activation_max;
  // Indexes are the offset to the memory buffer in the array used to keep track
  // of the allocated temporaries.
  int32_t im2col_index;
  int32_t hwcn_weights_index;
  int32_t input_quantized_index;
  int32_t scaling_factors_index;
  int32_t accum_scratch_index;
  int32_t input_offset_index;
  int32_t row_sums_index;

  bool need_hwcn_weights = false;
  bool have_weights_been_transposed = false;
  bool need_im2col = false;
  // If it's true, it means im2col is needed but gets disabled because the
  // temporary im2col tensor requires too much memory (i.e.
  // >= kMaxIm2colBufferSize);
  bool im2col_oversized = false;

  bool supports_multithreaded_kernel = false;
  bool is_hybrid_per_channel = false;
  bool compute_hybrid_row_sums = true;

  // Number of convolution groups.
  int32_t groups = 1;
};

inline PaddingType RuntimePaddingType(TfLitePadding padding) {
  switch (padding) {
    case TfLitePadding::kTfLitePaddingSame:
      return PaddingType::kSame;
    case TfLitePadding::kTfLitePaddingValid:
      return PaddingType::kValid;
    case TfLitePadding::kTfLitePaddingUnknown:
    default:
      return PaddingType::kNone;
  }
}

void ExtractConvParams(TfLitePadding padding, int stride_width, int stride_height, 
                               int dilation_width_factor, int dilation_height_factor,
                               TfLiteFusedActivation activation,
                               TfLiteConvParams* data_params) {
  // TfLiteConvParams data_params;
  data_params->padding = padding;
  data_params->stride_width = stride_width;
  data_params->stride_height = stride_height;
  data_params->dilation_width_factor = dilation_width_factor;
  data_params->dilation_height_factor = dilation_height_factor;
  data_params->activation = activation;
  // return data_params;
}

void GetConvTensor(TfLiteType type, const char* name, TfLiteIntArray* tensor_dims_data, 
                       TfLiteQuantizationParams quant_params,
                       char* tensor_data, TfLiteAffineQuantization* quant_struct,
                       size_t bytes_size, TfLiteTensor* tensor) {
  tensor->type = type;
  tensor->name = name;
  tensor->dims = tensor_dims_data;
  tensor->params = quant_params;
  // tensor->data.raw = reinterpret_cast<char*>(tensor_data);
  tensor->data.raw = tensor_data;
  tensor->bytes = bytes_size;
  tensor->allocation_type = kTfLiteMemNone;
  // data_0.allocation = allocation;
  tensor->is_variable = false;
  if (type != kTfLiteFloat32) {
    tensor->quantization.type = kTfLiteAffineQuantization;
    tensor->quantization.params = quant_struct;
  } else {
    tensor->quantization.type = kTfLiteNoQuantization;
  }
  tensor->sparsity = nullptr;
}

void* Init(TfLiteContext* context, const char* buffer, size_t length) {
  // This is a builtin op, so we don't use the contents in 'buffer', if any.
  // Instead, we allocate a new object to use as scratch space for im2col, and
  // to carry information from Prepare() to Eval().
  auto* data = new OpData;
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
  eigen_support::IncrementUsageCounter(context);
#endif
  return data;
}

void Free(TfLiteContext* context, void* buffer) {
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
  eigen_support::DecrementUsageCounter(context);
#endif
  delete reinterpret_cast<OpData*>(buffer);
}

// Naive implementation of transpose for floats. Could be optimized to be more
// cache friendly, but for now it's a one-time cost on first run, and we would
// prefer to remove the need to do this at all eventually.
void TransposeFloatTensor(const TfLiteTensor* input, TfLiteTensor* output) {
  const int rows = output->dims->data[1];
  const int cols = output->dims->data[0];
  const float* input_data = GetTensorData<float>(input);
  float* output_data = GetTensorData<float>(output);
  for (int i = 0; i < rows; ++i) {
    for (int j = 0; j < cols; ++j) {
      const float in_value = input_data[i * cols + j];
      output_data[j * rows + i] = in_value;
    }
  }
}

// Check if im2col needs to be allocated, as some version of optimized Conv dont
// use it. If any change is supporting im2col in any of the Conv versions, then
// it should be updated here as well
bool IsIm2ColRequired(const TfLiteTensor* input, TfLiteConvParams* params,
                      const TfLiteTensor* filter, OpData* data, bool is_hybrid,
                      KernelType kernel_type) {
  // If HWCN weights are required, Im2Col not required
  if (data->need_hwcn_weights) return false;

  // segregate based on dilated conv & non-dialated conv
  const bool need_dilated_im2col =
      params->dilation_width_factor != 1 || params->dilation_height_factor != 1;
  const bool need_non_dilated_im2col =
      params->stride_width != 1 || params->stride_height != 1 ||
      filter->dims->data[2] != 1 || filter->dims->data[1] != 1;

  const bool need_im2col = need_dilated_im2col || need_non_dilated_im2col;

  // Return early as basic requirement is not met
  if (!need_im2col) return false;

  // Special case for Hybrid, as it supports only non-dilated im2col currently
  const bool is_hybrid_non_dilated = is_hybrid && need_non_dilated_im2col;
  const bool is_quantized = input->type == kTfLiteUInt8 ||
                            input->type == kTfLiteInt8 ||
                            input->type == kTfLiteInt16;

  switch (kernel_type) {
    case kReference:
      if (is_hybrid) {
        return true;
      } else {
        return false;
      }
    case kGenericOptimized:
    case kCblasOptimized:
      if (is_hybrid && !need_non_dilated_im2col) {
        return false;
      } else {
        return true;
      }
    case kMultithreadOptimized:
      if (is_hybrid_non_dilated || is_quantized ||
          !data->supports_multithreaded_kernel) {
        return true;
      } else {
        return false;
      }
    default:
      return false;
  }
}

// Allocate temporary tensors (`im2col`, `hwcn_weights` if necessary).
// Note: `context->AddTensors` might invalidate pointers to existing tensors.
// Therefore the logic to add tensors are isolated into this function.
static TfLiteStatus AllocateTemporaryTensorsIfRequired(
    TfLiteContext* context, TfLiteNode* node, bool is_hybrid,
    bool is_per_channel, KernelType kernel_type, size_t im2col_bytes) {
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  TfLiteConvParams data_params;
  ExtractConvParams(paddings, stride_width, stride_height, dilation_width_factor, dilation_height_factor, activation, &data_params);
  TfLiteConvParams* params = &data_params;

  OpData* data = reinterpret_cast<OpData*>(node->user_data);

  // TF_LITE_ENSURE(context, node->inputs->size >= 2);
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  TfLiteTensor filter_tensor;
  TfLiteIntArray* filter_dims_data = TfLiteIntArrayCreate(filter_dims_size);
  int size_filter = 1;
  for (int i = 0; i < filter_dims_size; i++) {
    // std::cout << "dims_raw: " << dims_raw[i] << std::endl;
    filter_dims_data->data[i] = filter_dims_raw[i];
    size_filter *= filter_dims_raw[i];
  }
  size_t bytes_size_filter = sizeof(float) * size_filter;
  TfLiteQuantizationParams filter_params;
  filter_params.scale=scale_filter;
  filter_params.zero_point=zero_point_filter;

  TfLiteFloatArray* scale_array_filter = TfLiteFloatArrayCreate(1);
  scale_array_filter->data[0] = scale_filter;
  TfLiteIntArray* zero_point_array_filter = TfLiteIntArrayCreate(1);
  zero_point_array_filter->data[0] = zero_point_filter;

  TfLiteAffineQuantization quant_struct_filter;
  quant_struct_filter.scale = scale_array_filter;
  quant_struct_filter.zero_point = zero_point_array_filter;
  quant_struct_filter.quantized_dimension = 0;
  // float* filter_data;
  // filter_tensor_data = filter_raw;
  GetConvTensor(filter_type, "filter", filter_dims_data, filter_params,
                       reinterpret_cast<char*>(filter_tensor_data),
                       &quant_struct_filter, bytes_size_filter, &filter_tensor);
  const TfLiteTensor* filter = &filter_tensor;
  // TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 1, &filter));

  // If we're using the optimized multithreaded EigenTensor implementation of
  // convolution, it expects the filter weights to be transposed compared to
  // the normal TF Lite buffer format. Typical TF Lite weights are
  // [filter_count, filter_height, filter_width, input_depth], but for the float
  // implementation we need them as [filter_height, filter_width, input_depth,
  // filter_count]. We get to that format by transposing, and create a temporary
  // buffer to store the results.
  // This path is only used for float processing, so only create the buffer if
  // we're running with that data type.
  data->need_hwcn_weights =
      input->type == kTfLiteFloat32 && data->supports_multithreaded_kernel;

  // We don't always need to allocate im2col. It is only used in some versions
  // of the optimized Conv. This test just mimics something that happens inside
  // optimized_ops.h, in order to avoid a DCHECK(!im2col_data).
  data->need_im2col =
      IsIm2ColRequired(input, params, filter, data, is_hybrid, kernel_type);

  // If im2col_oversized is found to be true, we have to fallback to an
  // execution path (like kReference in float/quantized cases) that doesn't
  // require im2col operation. Therefore, we have to skip checking the hybrid
  // case (but not the hybrid-per-channel one) where there's no such a fallback
  // execution path.
  // TODO(b/178743262): Consider making this check conditioned on the available
  // memory of the system, rather than coupling to the mobile platform check.
  if (IsMobilePlatform() && !(is_hybrid && !is_per_channel) &&
      data->need_im2col && im2col_bytes >= kMaxIm2colBufferSizeMobile) {
    data->need_im2col = false;
    data->im2col_oversized = true;
  }
  int temporaries_count = 0;
  if (data->need_im2col) {
    data->im2col_index = temporaries_count;
    if (data->im2col_id == kTensorNotAllocated) {
      context->AddTensors(context, 1, &data->im2col_id);
    }
    ++temporaries_count;
  }
  if (data->need_hwcn_weights) {
    data->hwcn_weights_index = temporaries_count;
    if (data->hwcn_weights_id == kTensorNotAllocated) {
      context->AddTensors(context, 1, &data->hwcn_weights_id);
    }
    ++temporaries_count;
  }

  if (is_hybrid) {
    // Allocate tensor to store the on-the-fly quantized inputs.
    data->input_quantized_index = temporaries_count;
    if (data->input_quantized_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->input_quantized_id));
    }
    ++temporaries_count;

    // Allocate tensor to store the quantization params computed during
    // on-the-fly input quantization.
    data->scaling_factors_index = temporaries_count;
    if (data->scaling_factors_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->scaling_factors_id));
    }
    ++temporaries_count;

    // Allocate tensor to store the accumulators for the matrix multiply.
    data->accum_scratch_index = temporaries_count;
    if (data->accum_scratch_id == kTensorNotAllocated) {
      TF_LITE_ENSURE_OK(
          context, context->AddTensors(context, 1, &data->accum_scratch_id));
    }
    ++temporaries_count;
    if (is_per_channel) {
      data->input_offset_index = temporaries_count;
      if (data->input_offset_id == kTensorNotAllocated) {
        TF_LITE_ENSURE_OK(
            context, context->AddTensors(context, 1, &data->input_offset_id));
      }
      ++temporaries_count;

      data->row_sums_index = temporaries_count;
      if (data->row_sums_id == kTensorNotAllocated) {
        TF_LITE_ENSURE_OK(context,
                          context->AddTensors(context, 1, &data->row_sums_id));
      }
      ++temporaries_count;
    }
  }

  TfLiteIntArrayFree(node->temporaries);
  node->temporaries = TfLiteIntArrayCreate(temporaries_count);

  return kTfLiteOk;
}

TfLiteStatus Prepare(KernelType kernel_type, TfLiteContext* context,
                     TfLiteNode* node) {
  // std::cout << "codes runs here #-1" << std::endl;
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  TfLiteConvParams data_params;
  ExtractConvParams(paddings, stride_width, stride_height, dilation_width_factor, dilation_height_factor, activation, &data_params);
  TfLiteConvParams* params = &data_params;

  OpData* data = reinterpret_cast<OpData*>(node->user_data);
  // std::cout << "codes runs here #-2" << std::endl;
  bool has_bias = false;
  // Check number of inputs/outputs
  // TF_LITE_ENSURE(context, has_bias || node->inputs->size == 2);
  TF_LITE_ENSURE_EQ(context, node->outputs->size, 1);
  TfLiteTensor* output;
  TF_LITE_ENSURE_OK(context, GetOutputSafe(context, node, 0, &output));
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  // const TfLiteTensor* filter;
  // TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 1, &filter));
  // TfLiteTensor* filter;
  TfLiteTensor filter_tensor;
  TfLiteIntArray* filter_dims_data = TfLiteIntArrayCreate(filter_dims_size);
  int size_filter = 1;
  for (int i = 0; i < filter_dims_size; i++) {
    // std::cout << "dims_raw: " << dims_raw[i] << std::endl;
    filter_dims_data->data[i] = filter_dims_raw[i];
    size_filter *= filter_dims_raw[i];
  }
  size_t bytes_size_filter = sizeof(float) * size_filter;
  TfLiteQuantizationParams filter_params;
  filter_params.scale=scale_filter;
  filter_params.zero_point=zero_point_filter;

  TfLiteFloatArray* scale_array_filter = TfLiteFloatArrayCreate(1);
  scale_array_filter->data[0] = scale_filter;
  TfLiteIntArray* zero_point_array_filter = TfLiteIntArrayCreate(1);
  zero_point_array_filter->data[0] = zero_point_filter;

  TfLiteAffineQuantization quant_struct_filter;
  quant_struct_filter.scale = scale_array_filter;
  quant_struct_filter.zero_point = zero_point_array_filter;
  quant_struct_filter.quantized_dimension = 0;
  // float* filter_data;
  // filter_tensor_data = filter_raw;
  GetConvTensor(filter_type, "filter", filter_dims_data, filter_params,
                       reinterpret_cast<char*>(filter_tensor_data),
                       &quant_struct_filter, bytes_size_filter, &filter_tensor);
  const TfLiteTensor* filter = &filter_tensor;

  // Check dimensionality of input, filter
  TF_LITE_ENSURE_EQ(context, input->dims->size, 4);
  TF_LITE_ENSURE_EQ(context, filter->dims->size, 4);
  // Check input channels matching filter
  // Filter input channel can be a factor of channels of input (grouped conv)
  // or equals (normal conv).
  auto input_channel = input->dims->data[3];
  auto filter_input_channel = filter->dims->data[3];
  TF_LITE_ENSURE_EQ(context, input_channel % filter_input_channel, 0);
  data->groups = input_channel / filter_input_channel;
  // std::cout << "codes runs here #-3" << std::endl;
  // Check types. (We assume that UINT8 refers to quantized tensors)
  TfLiteType input_type = input->type;
  TF_LITE_ENSURE(context,
                 input_type == kTfLiteFloat32 || input_type == kTfLiteUInt8 ||
                     input_type == kTfLiteInt8 || input_type == kTfLiteInt16);
  TF_LITE_ENSURE_TYPES_EQ(context, output->type, input_type);

  if (input_type == kTfLiteInt16) {
    TF_LITE_ENSURE_EQ(context, input->params.zero_point, 0);
    TF_LITE_ENSURE_EQ(context, output->params.zero_point, 0);
  }
  // Filter must have zero zero-points in per-channel quantization.
  if (input_type == kTfLiteInt16 || input_type == kTfLiteInt8) {
    TF_LITE_ENSURE_EQ(context, filter->quantization.type,
                      kTfLiteAffineQuantization);
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    for (int i = 0; i < affine_quantization->zero_point->size; ++i) {
      TF_LITE_ENSURE_EQ(context, affine_quantization->zero_point->data[i], 0);
    }
  }
  // std::cout << "codes runs here #-4" << std::endl;
  const TfLiteTensor* bias = nullptr;

  // TODO(ahentz): At this point the optimized versions require 'bias'. We can
  // either change that or document that convolution requires it.
  // TF_LITE_ENSURE(context, has_bias);

  if (has_bias) {
    // TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 2, &bias));
    if (input_type == kTfLiteUInt8 || input_type == kTfLiteInt8) {
      TF_LITE_ENSURE_TYPES_EQ(context, bias->type, kTfLiteInt32);
      TF_LITE_ENSURE_EQ(context, bias->params.zero_point, 0);
    } else if (input_type == kTfLiteInt16) {
      TF_LITE_ENSURE(context, (bias->type == kTfLiteInt32) ||
                                  (bias->type == kTfLiteInt64));
      TF_LITE_ENSURE_EQ(context, bias->params.zero_point, 0);
    } else {
      TF_LITE_ENSURE_TYPES_EQ(context, bias->type, input_type);
    }
    TF_LITE_ENSURE_EQ(context, NumElements(bias), SizeOfDimension(filter, 0));
  }
  // std::cout << "codes runs here #-5" << std::endl;
  const bool is_hybrid =
      (input->type == kTfLiteFloat32 &&
       (filter->type == kTfLiteUInt8 || filter->type == kTfLiteInt8));

  if (is_hybrid && filter->type == kTfLiteInt8 &&
      filter->quantization.type == kTfLiteAffineQuantization &&
      filter->quantization.params &&
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params)
          ->scale &&
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params)
              ->scale->size > 1) {
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    const float scale = affine_quantization->scale->data[0];
    for (int i = 1; i < affine_quantization->scale->size; i++) {
      if (affine_quantization->scale->data[i] != scale) {
        data->is_hybrid_per_channel = true;
        break;
      }
    }
  }
  // std::cout << "codes runs here #-6" << std::endl;
  // The multi-threaded kernel supports neither dilation nor hybrid kernels, and
  // is incompatible with mutable input filters that might change between evals.
  data->supports_multithreaded_kernel =
      (kernel_type == kMultithreadOptimized) &&
      (context->recommended_num_threads != 1) && !is_hybrid &&
      (params->dilation_width_factor == 1) &&
      (params->dilation_height_factor == 1) &&
      (filter->allocation_type != kTfLiteArenaRw) && !IsDynamicTensor(filter);

  int channels_in = filter->dims->data[3];
  int channels_out = filter->dims->data[0];
  int width = input->dims->data[2];
  int height = input->dims->data[1];
  int filter_width = filter->dims->data[2];
  int filter_height = filter->dims->data[1];
  int batches = input->dims->data[0];
  // std::cout << "codes runs here #-7" << std::endl;
  // Matching GetWindowedOutputSize in TensorFlow.
  auto padding = params->padding;
  int out_width, out_height;
  data->padding = ComputePaddingHeightWidth(
      params->stride_height, params->stride_width,
      params->dilation_height_factor, params->dilation_width_factor, height,
      width, filter_height, filter_width, padding, &out_height, &out_width);

  size_t im2col_type_size;
  TF_LITE_ENSURE_STATUS(GetSizeOfType(context, input->type, &im2col_type_size));
  // Note that we intentionally promote the first multiplicand (i.e. 'batches')
  // to 'size_t' to avoid integer overflow here.
  const size_t im2col_bytes = static_cast<size_t>(batches) * out_height *
                              out_width * channels_in * filter_height *
                              filter_width * im2col_type_size;
  TF_LITE_ENSURE_STATUS(AllocateTemporaryTensorsIfRequired(
      context, node, is_hybrid, data->is_hybrid_per_channel, kernel_type,
      im2col_bytes));
  // std::cout << "codes runs here #-8" << std::endl;
  // TF_LITE_ENSURE(context, has_bias);

  // Note that full fixed-point inference requires that all tensors have their
  // parameters set. This is usually done during quantized training or
  // calibration.
  if (input_type != kTfLiteFloat32) {
    TF_LITE_ENSURE_EQ(context, filter->quantization.type,
                      kTfLiteAffineQuantization);
    const auto* affine_quantization =
        reinterpret_cast<TfLiteAffineQuantization*>(
            filter->quantization.params);
    TF_LITE_ENSURE(context, affine_quantization);
    TF_LITE_ENSURE(context, affine_quantization->scale);
    // std::cout << "affine_quantization->scale->size: " << affine_quantization->scale->size << std::endl;
    TF_LITE_ENSURE(context, (affine_quantization->scale->size == 1 ||
                             affine_quantization->scale->size == channels_out));

    data->per_channel_output_multiplier.resize(channels_out);
    data->per_channel_output_shift.resize(channels_out);
    TF_LITE_ENSURE_STATUS(tflite::PopulateConvolutionQuantizationParams(
        context, input, filter, bias, output, params->activation,
        &data->output_multiplier, &data->output_shift,
        &data->output_activation_min, &data->output_activation_max,
        data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), channels_out));
  }
  // std::cout << "codes runs here #-9" << std::endl;
  TfLiteIntArray* output_size = TfLiteIntArrayCreate(4);
  output_size->data[0] = batches;
  output_size->data[1] = out_height;
  output_size->data[2] = out_width;
  output_size->data[3] = channels_out;
  auto output_status = context->ResizeTensor(context, output, output_size);

  if (output_status != kTfLiteOk) return output_status;

  if (data->need_im2col) {
    node->temporaries->data[data->im2col_index] = data->im2col_id;

    TfLiteIntArray* im2col_size = TfLiteIntArrayCreate(4);

    auto filter_input_channel = filter->dims->data[3];
    im2col_size->data[0] = output_size->data[0];
    im2col_size->data[1] = output_size->data[1];
    im2col_size->data[2] = output_size->data[2];
    im2col_size->data[3] = filter_input_channel * filter_height * filter_width;

    TfLiteTensor* im2col =
        &context->tensors[node->temporaries->data[data->im2col_index]];
    im2col->type = input->type;
    if (is_hybrid) {
      im2col->type = filter->type;
    }
    im2col->allocation_type = kTfLiteArenaRw;
    auto im2col_status = context->ResizeTensor(context, im2col, im2col_size);
    if (im2col_status != kTfLiteOk) return im2col_status;
  }

  if (data->need_hwcn_weights) {
    node->temporaries->data[data->hwcn_weights_index] = data->hwcn_weights_id;
    TfLiteIntArray* hwcn_weights_size = TfLiteIntArrayCreate(2);

    // Because we're treating the filter weights as a matrix when we do the
    // transpose, we allocate the buffer with a two-dimensional shape, where one
    // dimension is the number of elements in each filter, and the second is the
    // total number of filters.
    auto filter_input_channel = filter->dims->data[3];
    hwcn_weights_size->data[0] =
        (filter_height * filter_width * filter_input_channel);
    hwcn_weights_size->data[1] = channels_out;

    TfLiteTensor* hwcn_weights =
        &context->tensors[node->temporaries->data[data->hwcn_weights_index]];
    hwcn_weights->type = input_type;
    hwcn_weights->allocation_type = kTfLiteArenaRwPersistent;

    auto hwcn_weights_status =
        context->ResizeTensor(context, hwcn_weights, hwcn_weights_size);
    if (hwcn_weights_status != kTfLiteOk) return hwcn_weights_status;

    // TODO(petewarden): If Resize() is called when the size hasn't actually
    // changed, this will do extra redundant work.
    data->have_weights_been_transposed = false;
  }

  if (is_hybrid) {
    node->temporaries->data[data->input_quantized_index] =
        data->input_quantized_id;
    TfLiteTensor* input_quantized;
    TF_LITE_ENSURE_OK(
        context, GetTemporarySafe(context, node, data->input_quantized_index,
                                  &input_quantized));
    input_quantized->type = kTfLiteInt8;
    input_quantized->allocation_type = kTfLiteArenaRw;
    if (!TfLiteIntArrayEqual(input_quantized->dims, input->dims)) {
      TfLiteIntArray* input_quantized_size = TfLiteIntArrayCopy(input->dims);
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, input_quantized,
                                                       input_quantized_size));
    }
    // std::cout << "codes runs here #-10" << std::endl;
    node->temporaries->data[data->scaling_factors_index] =
        data->scaling_factors_id;
    TfLiteTensor* scaling_factors;
    TF_LITE_ENSURE_OK(
        context, GetTemporarySafe(context, node, data->scaling_factors_index,
                                  &scaling_factors));
    scaling_factors->type = kTfLiteFloat32;
    scaling_factors->allocation_type = kTfLiteArenaRw;
    // Only one scale factor per batch is typically necessary. See optimized
    // implementation for why we need to allocate for the height of the inputs
    // flattened to 2D.
    TF_LITE_ENSURE(context, channels_in != 0);
    const int height = NumElements(input) / channels_in;
    int scaling_dims[1] = {height};
    if (!TfLiteIntArrayEqualsArray(scaling_factors->dims, 1, scaling_dims)) {
      TfLiteIntArray* scaling_factors_size = TfLiteIntArrayCreate(1);
      scaling_factors_size->data[0] = height;
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, scaling_factors,
                                                       scaling_factors_size));
    }

    node->temporaries->data[data->accum_scratch_index] = data->accum_scratch_id;
    TfLiteTensor* accum_scratch;
    TF_LITE_ENSURE_OK(context,
                      GetTemporarySafe(context, node, data->accum_scratch_index,
                                       &accum_scratch));
    accum_scratch->type = kTfLiteInt32;
    accum_scratch->allocation_type = kTfLiteArenaRw;
    const int scratch_width = batches * out_height * out_width;
    int accum_scratch_dims[2] = {channels_out, scratch_width};
    if (!TfLiteIntArrayEqualsArray(accum_scratch->dims, 2,
                                   accum_scratch_dims)) {
      TfLiteIntArray* accum_scratch_size = TfLiteIntArrayCreate(2);
      accum_scratch_size->data[0] = channels_out;
      accum_scratch_size->data[1] = scratch_width;
      TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, accum_scratch,
                                                       accum_scratch_size));
    }

    if (data->is_hybrid_per_channel) {
      const auto* affine_quantization =
          reinterpret_cast<TfLiteAffineQuantization*>(
              filter->quantization.params);
      TF_LITE_ENSURE_EQ(
          context, affine_quantization->scale->size,
          filter->dims->data[affine_quantization->quantized_dimension]);
      node->temporaries->data[data->input_offset_index] = data->input_offset_id;
      TfLiteTensor* input_offsets;
      TF_LITE_ENSURE_OK(
          context, GetTemporarySafe(context, node, data->input_offset_index,
                                    &input_offsets));
      input_offsets->type = kTfLiteInt32;
      input_offsets->allocation_type = kTfLiteArenaRw;
      // See above comment for the need to allocate for height of inputs.
      TF_LITE_ENSURE(context, channels_in != 0);
      const int height = NumElements(input) / channels_in;
      const int input_offset_dims[1] = {height};
      if (!TfLiteIntArrayEqualsArray(input_offsets->dims, 1,
                                     input_offset_dims)) {
        TfLiteIntArray* input_offsets_size = TfLiteIntArrayCreate(1);
        input_offsets_size->data[0] = input_offset_dims[0];
        TF_LITE_ENSURE_OK(context, context->ResizeTensor(context, input_offsets,
                                                         input_offsets_size));
      }
      node->temporaries->data[data->row_sums_index] = data->row_sums_id;
      TfLiteTensor* row_sums;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->row_sums_index, &row_sums));
      row_sums->type = kTfLiteInt32;
      row_sums->allocation_type = kTfLiteArenaRwPersistent;
      // See above comment for the need to allocate for height of inputs.
      const int row_sums_dims[1] = {channels_out};
      if (!TfLiteIntArrayEqualsArray(row_sums->dims, 1, row_sums_dims)) {
        TfLiteIntArray* row_sums_size = TfLiteIntArrayCreate(1);
        row_sums_size->data[0] = row_sums_dims[0];
        TF_LITE_ENSURE_OK(
            context, context->ResizeTensor(context, row_sums, row_sums_size));
      }
    }
  }
  // std::cout << "codes runs here #-11" << std::endl;
  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus Prepare(TfLiteContext* context, TfLiteNode* node) {
  return Prepare(kernel_type, context, node);
}

template <KernelType kernel_type>
void EvalQuantized(TfLiteContext* context, TfLiteNode* node,
                   TfLiteConvParams* params, OpData* data,
                   const TfLiteTensor* input, const TfLiteTensor* filter,
                   const TfLiteTensor* bias, TfLiteTensor* im2col,
                   TfLiteTensor* output) {
  auto input_offset = -input->params.zero_point;
  auto filter_offset = -filter->params.zero_point;
  auto output_offset = output->params.zero_point;

  KernelType effective_kernel_type;
  if ((kernel_type == kMultithreadOptimized ||
       kernel_type == kCblasOptimized) &&
      (params->dilation_width_factor != 1 ||
       params->dilation_height_factor != 1)) {
    // kMultithreadOptimized and kCblasOptimized do not support dilation.
    // Therefore, fallback to optimized.
    effective_kernel_type = kGenericOptimized;
  } else {
    effective_kernel_type = kernel_type;
  }

  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = PaddingType::kSame;
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.dilation_width_factor = params->dilation_width_factor;
  op_params.dilation_height_factor = params->dilation_height_factor;
  op_params.stride_width = params->stride_width;
  op_params.stride_height = params->stride_height;
  op_params.input_offset = input_offset;
  op_params.weights_offset = filter_offset;
  op_params.output_offset = output_offset;
  op_params.output_multiplier = data->output_multiplier;
  op_params.output_shift = -data->output_shift;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;
  switch (effective_kernel_type) {
    case kReference: {
      reference_ops::Conv(
          op_params, GetTensorShape(input), GetTensorData<uint8_t>(input),
          GetTensorShape(filter), GetTensorData<uint8_t>(filter),
          GetTensorShape(bias), GetTensorData<int32_t>(bias),
          GetTensorShape(output), GetTensorData<uint8_t>(output),
          GetTensorShape(im2col), GetTensorData<uint8_t>(im2col),
          /* cpu_backend_context = */ nullptr);
      break;
    }
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      // There is only one optimized implementation for Quantized Conv.
      optimized_ops::Conv(
          op_params, GetTensorShape(input), GetTensorData<uint8_t>(input),
          GetTensorShape(filter), GetTensorData<uint8_t>(filter),
          GetTensorShape(bias), GetTensorData<int32_t>(bias),
          GetTensorShape(output), GetTensorData<uint8_t>(output),
          GetTensorShape(im2col), GetTensorData<uint8_t>(im2col),
          CpuBackendContext::GetFromContext(context));
      break;
    }
  }
}

template <KernelType kernel_type>
void EvalQuantizedPerChannel(TfLiteContext* context, TfLiteNode* node,
                             TfLiteConvParams* params, OpData* data,
                             const TfLiteTensor* input,
                             const TfLiteTensor* filter,
                             const TfLiteTensor* bias, TfLiteTensor* output,
                             TfLiteTensor* im2col) {
  ConvParams op_params;
  op_params.input_offset = -input->params.zero_point;
  op_params.output_offset = output->params.zero_point;
  op_params.stride_height = params->stride_height;
  op_params.stride_width = params->stride_width;
  op_params.dilation_height_factor = params->dilation_height_factor;
  op_params.dilation_width_factor = params->dilation_width_factor;
  op_params.padding_values.height = data->padding.height;
  op_params.padding_values.width = data->padding.width;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  switch (effective_kernel_type) {
    case kReference: {
      reference_integer_ops::ConvPerChannel(
          op_params, data->per_channel_output_multiplier.data(),
          data->per_channel_output_shift.data(), GetTensorShape(input),
          GetTensorData<int8>(input), GetTensorShape(filter),
          GetTensorData<int8>(filter), GetTensorShape(bias),
          GetTensorData<int32>(bias), GetTensorShape(output),
          GetTensorData<int8>(output));
      break;
    }
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      optimized_integer_ops::ConvPerChannel(
          op_params, data->per_channel_output_multiplier.data(),
          data->per_channel_output_shift.data(), GetTensorShape(input),
          GetTensorData<int8>(input), GetTensorShape(filter),
          GetTensorData<int8>(filter), GetTensorShape(bias),
          GetTensorData<int32>(bias), GetTensorShape(output),
          GetTensorData<int8>(output), GetTensorShape(im2col),
          GetTensorData<int8>(im2col),
          CpuBackendContext::GetFromContext(context));
      break;
    }
  }
}

template <KernelType kernel_type>
void EvalQuantizedPerChannel16x8(TfLiteContext* context, TfLiteNode* node,
                                 TfLiteConvParams* params, OpData* data,
                                 const TfLiteTensor* input,
                                 const TfLiteTensor* filter,
                                 const TfLiteTensor* bias, TfLiteTensor* output,
                                 TfLiteTensor* im2col) {
  ConvParams op_params;
  op_params.input_offset = -input->params.zero_point;
  op_params.output_offset = output->params.zero_point;
  op_params.stride_height = params->stride_height;
  op_params.stride_width = params->stride_width;
  op_params.dilation_height_factor = params->dilation_height_factor;
  op_params.dilation_width_factor = params->dilation_width_factor;
  op_params.padding_values.height = data->padding.height;
  op_params.padding_values.width = data->padding.width;
  op_params.quantized_activation_min = data->output_activation_min;
  op_params.quantized_activation_max = data->output_activation_max;

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  // To prevent 32bit accum overflow for 16x8 quantization, it enables the
  // optimized path only when zero_point is 0.
  bool has_non_zero_point = input->params.zero_point ||
                            filter->params.zero_point ||
                            output->params.zero_point;

  // Fallback to reference kernel when bias_type is int64 as
  // there is no optimized kernel for int64 bias yet.
  if (bias && bias->type == kTfLiteInt64) {
    reference_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16>(input), GetTensorShape(filter),
        GetTensorData<int8>(filter), GetTensorShape(bias),
        GetTensorData<std::int64_t>(bias), GetTensorShape(output),
        GetTensorData<int16>(output));
  } else if (effective_kernel_type == kReference || has_non_zero_point) {
    reference_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16>(input), GetTensorShape(filter),
        GetTensorData<int8>(filter), GetTensorShape(bias),
        GetTensorData<std::int32_t>(bias), GetTensorShape(output),
        GetTensorData<int16>(output));
  } else {
    optimized_integer_ops::ConvPerChannel(
        op_params, data->per_channel_output_multiplier.data(),
        data->per_channel_output_shift.data(), GetTensorShape(input),
        GetTensorData<int16_t>(input), GetTensorShape(filter),
        GetTensorData<int8_t>(filter), GetTensorShape(bias),
        GetTensorData<std::int32_t>(bias), GetTensorShape(output),
        GetTensorData<int16_t>(output), GetTensorShape(im2col),
        GetTensorData<int16_t>(im2col),
        CpuBackendContext::GetFromContext(context));
  }
}

template <KernelType kernel_type>
void EvalFloat(TfLiteContext* context, TfLiteNode* node,
               TfLiteConvParams* params, OpData* data,
               const TfLiteTensor* input, const TfLiteTensor* filter,
               const TfLiteTensor* bias, TfLiteTensor* im2col,
               TfLiteTensor* hwcn_weights, TfLiteTensor* output) {
  // std::cout << "codes runs here #4" << std::endl;
  float output_activation_min, output_activation_max;
  CalculateActivationRange(params->activation, &output_activation_min,
                           &output_activation_max);
  KernelType effective_kernel_type = kernel_type;
  // Fall back to the optimized path if multi-threaded conv is unsupported.
  if ((kernel_type == kMultithreadOptimized) &&
      !data->supports_multithreaded_kernel) {
    effective_kernel_type = kGenericOptimized;
  }
  // std::cout << "codes runs here #5" << std::endl;
  // When im2col is needed (which is implied when 'im2col_oversized' is true),
  // the GEMMM-based optimized path requires im2col data be allocated to ensure
  // the correctness. Therefore, when im2col is disabled because of the
  // oversized temporary im2col tensor, fallback to a non-optimized path is
  // needed.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
    // As detailed by tflite::multithreaded_ops::Conv implementation in
    // multithreaded_conv.h, the Eigen-based execution doesn't need im2col data.
    // Therefore, we could rely on it as a better-optimized fallback than the
    // reference one.
    if (data->supports_multithreaded_kernel) {
      effective_kernel_type = kMultithreadOptimized;
    }
#endif
  }
  // std::cout << "codes runs here #6" << std::endl;
  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = RuntimePaddingType(params->padding);
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.stride_width = params->stride_width;
  op_params.stride_height = params->stride_height;
  op_params.dilation_width_factor = params->dilation_width_factor;
  op_params.dilation_height_factor = params->dilation_height_factor;
  op_params.float_activation_min = output_activation_min;
  op_params.float_activation_max = output_activation_max;
  switch (effective_kernel_type) {
    case kReference: {
      reference_ops::Conv(op_params, GetTensorShape(input),
                          GetTensorData<float>(input), GetTensorShape(filter),
                          GetTensorData<float>(filter), GetTensorShape(bias),
                          GetTensorData<float>(bias), GetTensorShape(output),
                          GetTensorData<float>(output), GetTensorShape(im2col),
                          GetTensorData<float>(im2col));
      break;
    }
    case kCblasOptimized:
    case kGenericOptimized: {
      optimized_ops::Conv(op_params, GetTensorShape(input),
                          GetTensorData<float>(input), GetTensorShape(filter),
                          GetTensorData<float>(filter), GetTensorShape(bias),
                          GetTensorData<float>(bias), GetTensorShape(output),
                          GetTensorData<float>(output), GetTensorShape(im2col),
                          GetTensorData<float>(im2col),
                          CpuBackendContext::GetFromContext(context));
      break;
    }
    case kMultithreadOptimized: {
#if defined(TFLITE_WITH_MULTITHREADED_EIGEN)
      // std::cout << "codes runs here #7" << std::endl;
      const float* filter_data;
      if (data->need_hwcn_weights) {
        filter_data = GetTensorData<float>(hwcn_weights);
      } else {
        filter_data = GetTensorData<float>(filter);
      }
      // int index;
      // for (index = 0; index < 432; index++){
      //   // std::cout << "filter_data[" << index << "] = " << filter_data[index] << std::endl;
      //   std::cout << filter_data[index] << ", ";
      // }
      multithreaded_ops::Conv(
          *eigen_support::GetThreadPoolDevice(context), op_params,
          GetTensorShape(input), GetTensorData<float>(input),
          GetTensorShape(filter), filter_data, GetTensorShape(bias),
          GetTensorData<float>(bias), GetTensorShape(output),
          GetTensorData<float>(output), GetTensorShape(im2col),
          GetTensorData<float>(im2col));
      break;
#else   // !defined(TFLITE_WITH_MULTITHREADED_EIGEN)
      // See Register_CONV_2D: we should never be here when TFLITE_WITH_RUY
      // was enabled. We #if out this code in order to get the corresponding
      // binary size benefits.
      TFLITE_DCHECK(false);
#endif  // defined(TFLITE_WITH_MULTITHREADED_EIGEN)
    }
  }
}

template <KernelType kernel_type>
TfLiteStatus EvalHybridPerChannel(TfLiteContext* context, TfLiteNode* node,
                                  TfLiteConvParams* params, OpData* data,
                                  const TfLiteTensor* input,
                                  const TfLiteTensor* filter,
                                  const TfLiteTensor* bias,
                                  TfLiteTensor* im2col, TfLiteTensor* output) {
  float output_activation_min, output_activation_max;
  CalculateActivationRange(params->activation, &output_activation_min,
                           &output_activation_max);

  const int batch_size = SizeOfDimension(input, 0);
  TF_LITE_ENSURE(context, batch_size != 0);
  const int input_size = NumElements(input) / batch_size;
  TfLiteTensor* quantized_input_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_quantized_index,
                                     &quantized_input_tensor));
  int8_t* quantized_input_ptr_batch =
      GetTensorData<int8_t>(quantized_input_tensor);
  TfLiteTensor* scaling_factors_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->scaling_factors_index,
                                     &scaling_factors_tensor));
  float* scaling_factors_ptr = GetTensorData<float>(scaling_factors_tensor);
  TfLiteTensor* input_offset_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_offset_index,
                                     &input_offset_tensor));
  int32_t* input_offset_ptr = GetTensorData<int32_t>(input_offset_tensor);

  for (int b = 0; b < batch_size; ++b) {
    const int offset = b * input_size;
    tensor_utils::AsymmetricQuantizeFloats(
        GetTensorData<float>(input) + offset, input_size,
        quantized_input_ptr_batch + offset, &scaling_factors_ptr[b],
        &input_offset_ptr[b]);
  }

  int8_t* im2col_ptr = nullptr;
  int8_t* filter_ptr = nullptr;
  if (im2col != nullptr) {
    im2col_ptr = im2col->data.int8;
  }
  filter_ptr = filter->data.int8;
  const auto* affine_quantization =
      reinterpret_cast<TfLiteAffineQuantization*>(filter->quantization.params);

  KernelType effective_kernel_type = kernel_type;
  // We have to fallback to reference execution path when im2col is needed but
  // disabled because to-be-allocated temporary im2col tensor is too large.
  // See b/178743262 for the detailed motivation.
  if (data->im2col_oversized) {
    effective_kernel_type = kReference;
  }

  // Grouped convolution is right now only supported on reference kernel.
  if (data->groups != 1) {
    effective_kernel_type = kReference;
  }

  ConvParams op_params;
  op_params.padding_type = PaddingType::kSame;
  op_params.padding_values.width = data->padding.width;
  op_params.padding_values.height = data->padding.height;
  op_params.dilation_width_factor = params->dilation_width_factor;
  op_params.dilation_height_factor = params->dilation_height_factor;
  op_params.stride_width = params->stride_width;
  op_params.stride_height = params->stride_height;
  op_params.float_activation_min = output_activation_min;
  op_params.float_activation_max = output_activation_max;
  switch (effective_kernel_type) {
    case kReference:
      reference_ops::HybridConvPerChannel(
          op_params, scaling_factors_ptr, GetTensorShape(input),
          quantized_input_ptr_batch, GetTensorShape(filter), filter_ptr,
          GetTensorShape(bias), GetTensorData<float>(bias),
          GetTensorShape(output), GetTensorData<float>(output),
          GetTensorShape(im2col), im2col_ptr, affine_quantization->scale->data,
          input_offset_ptr);
      break;
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      TfLiteTensor* row_sums;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->row_sums_index, &row_sums));
      TfLiteTensor* scratch;
      TF_LITE_ENSURE_OK(
          context,
          GetTemporarySafe(context, node, data->accum_scratch_index, &scratch));
      optimized_ops::HybridConvPerChannel(
          op_params, scaling_factors_ptr, GetTensorShape(input),
          quantized_input_ptr_batch, GetTensorShape(filter), filter_ptr,
          GetTensorShape(bias), GetTensorData<float>(bias),
          GetTensorShape(output), GetTensorData<float>(output),
          GetTensorShape(im2col), im2col_ptr, affine_quantization->scale->data,
          input_offset_ptr, GetTensorShape(scratch),
          GetTensorData<int32>(scratch), GetTensorData<int32_t>(row_sums),
          &data->compute_hybrid_row_sums,
          CpuBackendContext::GetFromContext(context));
      data->compute_hybrid_row_sums = false;
      break;
    }
  }

  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus EvalHybrid(TfLiteContext* context, TfLiteNode* node,
                        TfLiteConvParams* params, OpData* data,
                        const TfLiteTensor* input, const TfLiteTensor* filter,
                        const TfLiteTensor* bias, TfLiteTensor* im2col,
                        TfLiteTensor* accum_scratch, TfLiteTensor* output) {
  float output_activation_min, output_activation_max;
  CalculateActivationRange(params->activation, &output_activation_min,
                           &output_activation_max);

  const int batch_size = SizeOfDimension(input, 0);
  TF_LITE_ENSURE(context, batch_size != 0);
  const int input_size = NumElements(input) / batch_size;

  const float* input_ptr = GetTensorData<float>(input);
  TfLiteTensor* quantized_input_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->input_quantized_index,
                                     &quantized_input_tensor));
  int8_t* quantized_input_ptr_batch =
      GetTensorData<int8_t>(quantized_input_tensor);
  TfLiteTensor* scaling_factors_tensor;
  TF_LITE_ENSURE_OK(context,
                    GetTemporarySafe(context, node, data->scaling_factors_index,
                                     &scaling_factors_tensor));
  float* scaling_factors_ptr = GetTensorData<float>(scaling_factors_tensor);

  // Per-batch input quantization for higher accuracy.
  {
    ruy::profiler::ScopeLabel label("ConvHybridQuantizeInputs");
    for (int b = 0; b < batch_size; ++b) {
      float unused_min, unused_max;
      const int offset = b * input_size;
      tensor_utils::SymmetricQuantizeFloats(
          input_ptr + offset, input_size, quantized_input_ptr_batch + offset,
          &unused_min, &unused_max, &scaling_factors_ptr[b]);
      scaling_factors_ptr[b] *= filter->params.scale;
    }
  }

  switch (kernel_type) {
    case kReference:
    case kGenericOptimized:
    case kMultithreadOptimized:
    case kCblasOptimized: {
      // There is only one implementation for hybrid kernel.
      ConvParams op_params;
      op_params.padding_type = PaddingType::kSame;
      op_params.padding_values.width = data->padding.width;
      op_params.padding_values.height = data->padding.height;
      op_params.stride_width = params->stride_width;
      op_params.stride_height = params->stride_height;
      op_params.dilation_width_factor = params->dilation_width_factor;
      op_params.dilation_height_factor = params->dilation_height_factor;
      op_params.float_activation_min = output_activation_min;
      op_params.float_activation_max = output_activation_max;
      if (data->groups == 1) {
        optimized_ops::HybridConv(
            op_params, scaling_factors_ptr, GetTensorShape(input),
            quantized_input_ptr_batch, GetTensorShape(filter),
            GetTensorData<int8_t>(filter), GetTensorShape(bias),
            GetTensorData<float>(bias), GetTensorShape(accum_scratch),
            GetTensorData<int32_t>(accum_scratch), GetTensorShape(output),
            GetTensorData<float>(output), GetTensorShape(im2col),
            GetTensorData<int8_t>(im2col),
            CpuBackendContext::GetFromContext(context));
      } else {
        // This case is handled by (fallbacked to) per channel hybrid group conv
        // and shouldn't hit this branch.
        TF_LITE_KERNEL_LOG(
            context,
            "Group convolution currently not supported for hybrid kernel.");
        return kTfLiteError;
      }
      break;
    }
  }

  return kTfLiteOk;
}

template <KernelType kernel_type, TfLiteType input_type>
TfLiteStatus EvalImpl(TfLiteContext* context, TfLiteNode* node) {
  // std::cout << "codes runs here #0" << std::endl;
  // auto* params = reinterpret_cast<TfLiteConvParams*>(node->builtin_data);
  TfLiteConvParams data_params;
  ExtractConvParams(paddings, stride_width, stride_height, dilation_width_factor, dilation_height_factor, activation, &data_params);
  TfLiteConvParams* params = &data_params;

  OpData* data = reinterpret_cast<OpData*>(node->user_data);
  // std::cout << "codes runs here #1" << std::endl;
  TfLiteTensor* output;
  TF_LITE_ENSURE_OK(context, GetOutputSafe(context, node, 0, &output));
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));
  TfLiteTensor filter_tensor;
  TfLiteIntArray* filter_dims_data = TfLiteIntArrayCreate(filter_dims_size);
  int size_filter = 1;
  for (int i = 0; i < filter_dims_size; i++) {
    // std::cout << "dims_raw: " << dims_raw[i] << std::endl;
    filter_dims_data->data[i] = filter_dims_raw[i];
    size_filter *= filter_dims_raw[i];
  }
  size_t bytes_size_filter = sizeof(float) * size_filter;
  TfLiteQuantizationParams filter_params;
  filter_params.scale=scale_filter;
  filter_params.zero_point=zero_point_filter;

  TfLiteFloatArray* scale_array_filter = TfLiteFloatArrayCreate(1);
  scale_array_filter->data[0] = scale_filter;
  TfLiteIntArray* zero_point_array_filter = TfLiteIntArrayCreate(1);
  zero_point_array_filter->data[0] = zero_point_filter;

  TfLiteAffineQuantization quant_struct_filter;
  quant_struct_filter.scale = scale_array_filter;
  quant_struct_filter.zero_point = zero_point_array_filter;
  quant_struct_filter.quantized_dimension = 0;

  // float* filter_data;
  // filter_tensor_data = filter_raw;
  GetConvTensor(filter_type, "filter", filter_dims_data, filter_params,
                       reinterpret_cast<char*>(filter_tensor_data), 
                       &quant_struct_filter, bytes_size_filter, &filter_tensor);
  const TfLiteTensor* filter = &filter_tensor;

  TfLiteTensor bias_tensor;
  const TfLiteTensor* bias;
  if (has_conv_bias) {
    TfLiteIntArray* bias_dims_data = TfLiteIntArrayCreate(bias_dims_size);
    int size_bias = 1;
    for (int i = 0; i < bias_dims_size; i++) {
      // std::cout << "dims_raw: " << dims_raw[i] << std::endl;
      bias_dims_data->data[i] = bias_dims_raw[i];
      size_bias *= bias_dims_raw[i];
    }
    size_t bytes_size_bias = sizeof(float) * size_bias;
    TfLiteQuantizationParams bias_params;
    bias_params.scale=scale_bias;
    bias_params.zero_point=zero_point_bias;

    TfLiteFloatArray* scale_array_bias = TfLiteFloatArrayCreate(1);
    scale_array_bias->data[0] = scale_bias;
    TfLiteIntArray* zero_point_array_bias = TfLiteIntArrayCreate(1);
    zero_point_array_bias->data[0] = zero_point_bias;

    TfLiteAffineQuantization quant_struct_bias;
    quant_struct_bias.scale = scale_array_bias;
    quant_struct_bias.zero_point = zero_point_array_bias;
    quant_struct_bias.quantized_dimension = 0;
    
    // float* bias_data;
    // bias_tensor_data = bias_raw;
    GetConvTensor(bias_type, "bias", bias_dims_data, bias_params,
                        reinterpret_cast<char*>(bias_tensor_data), 
                        &quant_struct_bias, bytes_size_bias, &bias_tensor);
    bias = &bias_tensor;
  } else {
    bias = nullptr;
  }

  TfLiteTensor* im2col =
      data->need_im2col
          ? &context->tensors[node->temporaries->data[data->im2col_index]]
          : nullptr;
  TfLiteTensor* hwcn_weights =
      data->need_hwcn_weights
          ? &context->tensors[node->temporaries->data[data->hwcn_weights_index]]
          : nullptr;

  if (data->need_hwcn_weights && !data->have_weights_been_transposed) {
    TransposeFloatTensor(filter, hwcn_weights);
    data->have_weights_been_transposed = true;
  }
  // std::cout << "codes runs here #3" << std::endl;
  TFLITE_DCHECK_EQ(input_type, input->type);
  switch (input_type) {  // Already know in/outtypes are same.
    case kTfLiteFloat32:
      if (filter->type == kTfLiteUInt8 || filter->type == kTfLiteInt8) {
        if (data->is_hybrid_per_channel ||
            // TODO(b/162870360): Fallback to PerChannel implementation
            // before we have grouped hybrid convolution.
            data->groups != 1) {
          TF_LITE_ENSURE_OK(context, EvalHybridPerChannel<kernel_type>(
                                         context, node, params, data, input,
                                         filter, bias, im2col, output));
        } else {
          TfLiteTensor* accum_scratch =
              &context->tensors[node->temporaries
                                    ->data[data->accum_scratch_index]];
          TF_LITE_ENSURE_OK(context,
                            EvalHybrid<kernel_type>(context, node, params, data,
                                                    input, filter, bias, im2col,
                                                    accum_scratch, output));
        }
      } else {
        EvalFloat<kernel_type>(context, node, params, data, input, filter, bias,
                               im2col, hwcn_weights, output);
      }
      break;
    case kTfLiteUInt8:
      EvalQuantized<kernel_type>(context, node, params, data, input, filter,
                                 bias, im2col, output);
      break;
    case kTfLiteInt8:
      EvalQuantizedPerChannel<kernel_type>(context, node, params, data, input,
                                           filter, bias, output, im2col);
      break;
    case kTfLiteInt16:
      EvalQuantizedPerChannel16x8<kernel_type>(
          context, node, params, data, input, filter, bias, output, im2col);
      break;
    default:
      TF_LITE_KERNEL_LOG(context, "Type %s currently not supported.",
                         TfLiteTypeGetName(input->type));
      return kTfLiteError;
  }
  // std::cout << "codes runs here #10" << std::endl;
  return kTfLiteOk;
}

template <KernelType kernel_type>
TfLiteStatus Eval(TfLiteContext* context, TfLiteNode* node) {
  const TfLiteTensor* input;
  TF_LITE_ENSURE_OK(context, GetInputSafe(context, node, 0, &input));

  switch (input->type) {
    case kTfLiteFloat32:
      return EvalImpl<kernel_type, kTfLiteFloat32>(context, node);
    case kTfLiteUInt8:
      return EvalImpl<kernel_type, kTfLiteUInt8>(context, node);
    case kTfLiteInt8:
      return EvalImpl<kernel_type, kTfLiteInt8>(context, node);
    case kTfLiteInt16:
      return EvalImpl<kernel_type, kTfLiteInt16>(context, node);
    default:
      TF_LITE_KERNEL_LOG(context, "Type %s not currently supported.",
                         TfLiteTypeGetName(input->type));
      return kTfLiteError;
  }
}

}  // namespace conv

TfLiteRegistration* Register_vcgkru_REF() {
  static TfLiteRegistration r = {vcgkru::Init, vcgkru::Free,
                                 vcgkru::Prepare<vcgkru::kReference>,
                                 vcgkru::Eval<vcgkru::kReference>};
  return &r;
}

TfLiteRegistration* Register_vcgkru_GENERIC_OPT() {
  static TfLiteRegistration r = {vcgkru::Init, vcgkru::Free,
                                 vcgkru::Prepare<vcgkru::kGenericOptimized>,
                                 vcgkru::Eval<vcgkru::kGenericOptimized>};
  return &r;
}

TfLiteRegistration* Register_vcgkru_MULTITHREADED_OPT() {
  static TfLiteRegistration r = {vcgkru::Init, vcgkru::Free,
                                 vcgkru::Prepare<vcgkru::kMultithreadOptimized>,
                                 vcgkru::Eval<vcgkru::kMultithreadOptimized>};
  return &r;
}

// TfLiteRegistration* Register_vcgkru_CBLAS_OPT() {
//   static TfLiteRegistration r = {vcgkru::Init, vcgkru::Free,
//                                  vcgkru::Prepare<vcgkru::kCblasOptimized>,
//                                  vcgkru::Eval<vcgkru::kCblasOptimized>};
//   return &r;
// }

TfLiteRegistration* Register_vcgkru() {
#if defined TFLITE_WITH_MULTITHREADED_EIGEN
  return Register_vcgkru_MULTITHREADED_OPT();
#else
  return Register_vcgkru_GENERIC_OPT();
#endif
}


}  // namespace builtin
}  // namespace ops
}  // namespace tflite
